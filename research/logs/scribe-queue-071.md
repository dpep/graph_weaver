# Queued for the 0.7.1 scribe pass (docs only; verify each against code before writing)

From senior U (scalar author owning both sides; log senior-log-U.md, app senior-app-U):
1. docs/scalars.md, a short "What no check can see" section: a scalar's wire contract lives in the server's coerce_input/coerce_result, which the schema dump does not carry, so a server-only coercer change (Decimal string → JSON number, same scalar name) passes verify, schema:diff and generate, and at runtime BigDecimal(Float) silently loses precision past ~7 significant digits. The lockstep check is a `:in_process` example per scalar that round-trips one value through the real schema class — give the four-line recipe. Say that the `:fake` fabricates from the client registration only, so it can pass a value the real server rejects; `:in_process` is what catches it.
2. docs/scalars.md, beside the money shapes: an asymmetric scalar (object out, string in) breaks `from_h(JSON.parse(x.to_json)) == x` because `register_scalar`'s one `serialize:` feeds both the outbound variable and `as_json`; name it as the documented bound (a design question for later, on the post-release list), and say which pin shape `:fake` wants for the result direction (the object form, i.e. what the server sends).
3. docs/scalars.md: "Money as a bare decimal" is a registration a given server may flatly reject; nothing before a real request says so — the same `:in_process` recipe is the check.

From senior R (checkout engineer) — the writes lane is doing R2 and R6 itself; nothing queued.

From senior P (concurrency) — the concurrency lane owns its docs; nothing queued.

From senior T (subgraph author; log senior-log-T.md, app senior-app-T):
4. docs/federation.md, the federation:diff section AND the task's own success sentence (lib/graph_weaver/federation.rb or internal/tasks.rb — small code change: "matches the schemas here" → say what was compared): diff answers "did you forget to recompose", never "would the next recompose succeed" — a @key added/removed/made unresolvable and a same-name non-shareable field collision all report clean, because the check is field-coordinate presence and type; a genuinely new field IS caught. The message should name the two categories it doesn't check. Spec the message.
5. docs/federation.md, a recipe: a subgraph whose resolver calls the composed supergraph needs its own real, resolvable client target in production — every test mode (:router, :wire, :in_process) intercepts exactly that call, so four green tiers prove nothing about it; the senior's app 500'd under a real gateway with a DNS failure. Show the shape (a dedicated graph with its own `client`, distinct from the app's outbound one) and say why the modes can't see it.
6. docs/federation.md, the apollo-federation footguns paragraph: two `@key`s on one `extend_type` type plus any `@requires`/`@provides` on that type crashes @apollo/composition ("Unexpected element: federation__provides"), independent of which fields — a version interaction, one sentence.
7. docs/federation.md, representations: the client-side Representations builder can validate incoming `_entities` representations server-side (better messages than apollo-federation-ruby's), with two gotchas — a single-key entity's builder takes required kwargs so a raw hash with a missing/extra field raises Ruby's ArgumentError not InputError, and with overlapping key sets the first satisfied set wins silently. A footnote, not a feature.
