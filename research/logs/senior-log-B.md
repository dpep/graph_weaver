# Senior engineer evaluation log — graph_weaver (session B)

20:23:26 — session start, read docs/errors.md, docs/i18n.md, docs/testing.md, docs/logging.md (filter_parameters section), docs/getting_started.md
20:26:58 — reading lib/graph_weaver/railtie.rb and lib/graph_weaver.rb: skipped 'rails g graph_weaver:install' (hand-rolled schema+initializer instead), need to confirm default schema_path/generated_paths/queries_paths and whether install generator does anything beyond what I already wrote by hand
20:27:38 — hit Zeitwerk autoload-order bug: validators (installed via GraphQL::Schema::Validator.install, referenced only by symbol) weren't loaded before the schema referenced them; two separate initializers race on alphabetical load order. Not a graph_weaver bug -- a graphql-ruby+Zeitwerk interaction -- but exactly the class of host-seam gotcha CLAUDE.md's own playbook warns about. Fixed by loading validators in the same to_prepare block, before referencing the schema class.
20:30:22 — read lib/graph_weaver/client.rb and lib/graph_weaver/schema_loader.rb (grep for introspect/isOneOf) after noticing the generated GraphQLTypes::PaymentInput carries no ONE_OF const even though app/graphql/types/payment_input.rb declares 'one_of'. Traced it to GraphQL::Introspection.query (graphql-ruby 2.6.10) defaulting include_is_one_of: false, and graph_weaver's SchemaLoader.introspect calling GraphQL::Introspection.query with no args -- so isOneOf never survives introspection, whether against a remote URL or an in-process schema class. Candidate bug: client-side @oneOf enforcement is silently inert for any schema learned via introspection (the documented, primary install path), and only works when the schema is loaded from committed SDL. Will confirm empirically in specs.
20:32:18 — confirmed via lib/graph_weaver/input_struct.rb (serialize calls one_of! only if self.class.const_defined?(:ONE_OF, false)) and lib/graph_weaver/codegen/emit.rb (ONE_OF only emitted when node.one_of) that PaymentInput's missing ONE_OF const means client-side oneOf enforcement is dead for this app. Root cause confirmed: GraphWeaver::SchemaLoader.introspect calls GraphQL::Introspection.query with no args (client.rb), and that helper defaults include_is_one_of: false (graphql-ruby 2.6.10), so isOneOf never reaches the dump. Moving to writing specs.
20:42:35 — direct rails runner probing (not a spec) to characterize graphql-ruby 2.6.10's validates: error-count behavior: a request with TWO invalid arguments (coupon format + line qty range, both via custom Validators raising GraphQL::ExecutionError) surfaces only ONE error, not both; likewise two bad line items in one list only surfaces the first. graphql-ruby appears to abort argument validation at the first raised ExecutionError rather than collecting every validator's failure across the tree, unlike ActiveModel::Errors. This directly answers the task's 'does the server send many' question: no, not for this recipe -- a form built on this can't assume one round trip surfaces every mistake. Verified this isn't a graph_weaver limitation (it reads whatever errors array it's given, and its own Response#input_errors is correctly plural) -- it's upstream in graphql-ruby's own validator execution. Fixing server_side_input_errors_spec.rb to match reality.
20:43:35 — all 54 specs green. Running with --order rand a couple of seeds for order-independence.
20:44:19 — finished the spec suite (54 examples, 0 failures across seeds 1/99/default). Moving to compiling the final report.

## Summary of files produced

- Schema/app code: app/graphql/{senior_app_schema.rb, types/*, mutations/create_order.rb, validators/*, queries/create_order.graphql}
- Generated: app/graphql/generated/* (via rake graph_weaver:generate, committed as the gem expects)
- Config: config/initializers/graph_weaver.rb
- Specs: spec/graphql/{client_input_errors,server_side_input_errors,simulated_servers,filter_parameters,i18n,execute_and_form_mapping}_spec.rb (54 examples, 0 failures, stable across --order rand seeds 1/99)

## lib/spec reads and why (see inline log entries above for exact triggers)

1. docs/errors.md, docs/i18n.md, docs/testing.md, docs/logging.md, docs/getting_started.md — required starting points per task.
2. lib/graph_weaver.rb (queries_paths/generated_paths/schema_path defaults) — confirmed hand-rolling the schema without the install generator would still land in the paths graph_weaver expects; docs describe the generator's output but not the bare defaults explicitly enough to skip verifying.
3. lib/graph_weaver/input_struct.rb — needed the exact mechanics of #coerce/#serialize/one_of! to write precise hostile-input assertions (prop vs wire spelling, when a coercer runs, how @oneOf is actually enforced) rather than guessing from prose.
4. lib/graph_weaver/errors.rb (InputError, GraphQLError#input_errors, KINDS/DETAILS) — confirmed to_h shape, KINDS list, DETAILS whitelist precisely.
5. lib/graph_weaver/internal/server_input.rb — the ONLY way to know precisely how hostile extensions.input payloads (bad kind, non-array path, wrong-typed details) degrade; docs describe the happy path and the general principle ("dropped rather than passed through") but not the exact fallback path taken for path specifically.
6. lib/graph_weaver/coerce.rb — to know which values produce :type_mismatch vs :unparseable (a String vs a non-String), since a first guess based on docs prose alone was wrong twice in the spec-writing pass.
7. lib/graph_weaver/logging.rb (Redact/filter_variables/scrub) — to test filter_parameters' actual matching rule (substring, case-insensitive, recursive into Hash/Array) rather than assume "fuzzy" matching.
8. lib/graph_weaver/client.rb + lib/graph_weaver/schema_loader.rb — traced the @oneOf/isOneOf finding to GraphWeaver::SchemaLoader.introspect calling GraphQL::Introspection.query with no args.
9. lib/graph_weaver/codegen.rb + codegen/emit.rb — confirmed ONE_OF is only emitted when node.one_of, which is read off the introspected/dumped schema, not the live class — closing the loop on the oneOf finding.
10. graphql-ruby gem source (lib/graphql/introspection.rb) — confirmed GraphQL::Introspection.query's include_is_one_of: false default; this is upstream, not graph_weaver, but necessary to know whose bug it is.
