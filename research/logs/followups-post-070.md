# After 0.7.0 ships (do not fold into the release)

- (DONE by senior O: full-corpus outbound sweep, 3146 trips, only the documented sub-microsecond Time bound) Round-trip harness: `bin/round-trip` proves only the `from_h` direction; add the `as_json` trip (`from_h(JSON.parse(x.to_json)) == x`) to `spec/support/round_trip.rb` — the lane's throwaway sweep is at /tmp/claude/graph_weaver/as-json-sweep.rb and found only the documented sub-microsecond Time bound.
- `details[:type]` on an InputError speaks Sorbet ("T::Array[Float]") for a field with no coercer and GraphQL ("Money") for a registered one; making it uniformly schema vocabulary needs the GraphQL type name in the generated FIELDS row (emitter change).
- `register_scalar` with `cast:` and no `serialize:` gets no registration-time warning beside `warn_half_a_value_object` (scalar_type.rb).
- Corpus sweep leftovers: GitLab's 72 enum-value case collisions refuse with `register_enum` as the remedy on a user's first sorted query — consider a friendlier default; the fuzzer builds queries the field-merging rule forbids (graphql-ruby warns today, will reject) — fix the fuzzer before that flips.
- Hasura `where:` value wrong for the column fails at Postgres (`data-exception`) — a third Hasura code to read, if it proves common.
- CHANGELOG.md `merge=union` gitattribute so lane rebases stop conflicting on it.
- Two lanes each saw a one-off burst of local-server spec failures in a full run (16 in http_spec.rb, 8 in logging_spec.rb, "HTTP 404" from the stub server) that never reproduced alone or on re-run, both while another process (npm install / a worktree suite) ran concurrently. Likely fixed-port contention in the stub servers; check whether every local server binds port 0 and hands its port to the spec.
- (senior K) Persisted queries: no `extensions.persistedQuery` sent and no hook; a `rake graph_weaver:persisted_queries:manifest` task built from the generated modules' QUERY/OPERATION_NAME constants (~10 lines) and a `persisted_queries: true` transport option doing APQ register-on-miss (~12-line Transport::HTTP subclass proved it). Feature for 0.8.
- (senior K) `queries:check`/`schema:diff` against a supergraph URL so a federated app has a pre-deploy check against the live graph; today all three CI tasks compare the app to its own artifacts.
- (senior K) Correlation: the `execute.graph_weaver` payload carries no request/trace id and `Response` keeps no response headers; a caller-supplied correlation id in the payload would close the response-side half.
- (senior M) File uploads: the GraphQL multipart request spec is unsupported; 0.7.0 refuses a File/IO variable by name (K transport lane). A `multipart: true` transport path that extracts IO values into the multipart map is the feature.
- (senior J) `list_size` per-coordinate landed; consider the same Hash form for `null_chance`.
- (J lane, measured) The built-in DateTime/ISO8601DateTime scalars register as Time with cast: :parse, the slowest of the four timestamp casts (7× Date.iso8601, 4.5× DateTime.iso8601); `cast: :iso8601` is ~3× cheaper and stricter. Consider making :iso8601 the default for ISO8601DateTime (a behavior change: looser forms would start refusing) — decide with a measurement of what servers actually send.
- (junior 15, packaged gem) `examples/` is excluded from the gem (gemspec `git ls-files ... ':!:examples'`) while the README says "the examples run that path for real" and links them — either ship the directory (small text files) or reword the README to say they live in the repo. RubyGems.org renders no README, only the one-line summary; the gem page is a dead end without the homepage link — consider a `metadata["documentation_uri"]`/`changelog_uri` and a README first paragraph that survives that.
- (junior 15) `require "graph_weaver/log_subscriber"` standalone NameErrors without `active_support/log_subscriber` loaded first; a one-line require inside makes it standalone-safe.
- (senior O) `stub_graphql(key).to_return(value)` as the pin verb, coordinates first, operation scoping optional and deferred — dogfood lane running; promote in a minor version with the graphql_fake split if it earns it.
- (senior O) Corpus rerun on the published gem: zero regressions across 22 schemas × 6 seeds + hostile; the one new bug (dotted/kebab query file names) has a 0.7.1 lane.
- (stub_graphql dogfood, DECIDED don't ship) `stub_graphql(key).to_return(value)`: 7 of 17 pin sites converted, 5 marginally better, 2 worse (under :wire the verb no longer says the mode), 10 couldn't (no schema: door, options, mode); `to_return` carries no information `=>` doesn't; the split would also have to move pins out of graphql_router(fake:) and config.overrides. Branch kept as `experiment/stub-graphql` for the record; the two object-pin bug fixes it found are on main (df7043b).
- (stub_graphql dogfood) An object pin holding a registered scalar whose Ruby class is already JSON-shaped (BigDecimal Money with serialize: [:to_s, "F"]) reaches the wire unserialized — a JSON number, "12.5" not "12.50" — because Values#wire returns already-wire-shaped values as written (the rule that makes a hand-written "Money" => "12.00" work). Needs the object path told apart from a hand-written pin; design question, own lane.
- (senior U) `specifiedByURL` never reaches an introspected dump: `GraphQL::Introspection.query` isn't asked with `include_specified_by_url: true` (same shape as the isOneOf fix; the ask-then-fallback exists). One line in schema_loader.rb's `ask` plus checking the loader keeps it; DO IN 0.7.1 after the concurrency lane (which owns schema_loader's cache_path) lands.
- (senior U) An asymmetric scalar (object out, string in) can't round-trip through as_json because one `serialize:` serves both the outbound variable and the result's JSON; a `to_wire:`/`as_json:` split is a knob — decide with a real user.
- (senior S, DECLINED by the writes lane with a measured argument, I agree) A CastError closes the `execute.graph_weaver` event as :ok and then raises, and Testing doubles emit no events. Moving the extent up to QueryModule#dispatch would collapse Retry's per-attempt events (`:retries`, per-attempt `:http_status`) into one — a contract rewrite, not a fix. The shape to do deliberately later: keep the per-attempt event and add a caller-outcome event (or a `cast` phase) emitted at the module seam for every client kind, with its own docs pass. Meanwhile a CastError does reach the warn log.
