# Junior 15 log — graph_weaver 0.7.0 from RubyGems, cold start

Persona: Rails dev who found graph_weaver on RubyGems this morning. No checkout, no earlier logs read (per brief). Everything below is from the gem page, its README, and whatever docs it links to.

15:33 - start. gem page confirms graph_weaver 0.7.0 live on rubygems.org, homepage/source https://github.com/dpep/graph_weaver, documentation_uri -> https://github.com/dpep/graph_weaver/tree/main/docs
15:40 - creating rails app junior-app-15 with rails new (skip default test framework not specified in docs, keep defaults + rspec since docs assume rspec)
15:35-15:37 - `rails new junior-app-15 --minimal --skip-test` (Rails 8.1.3.1), added `gem "graph_weaver"` and `gem "rspec-rails"` (dev/test) to Gemfile, `bundle install`. Bundler resolved **graph_weaver 0.7.0** (latest on rubygems.org, released today per gem page). No dependency conflicts. graphql resolved to 2.6.10.
15:36 - `rails g rspec:install`, then `rails g graph_weaver:install https://countries.trevorblades.com/graphql`. Worked first try exactly as the getting_started.md transcript shows: wrote config/initializers/graph_weaver.rb, app/graphql/queries/.keep, app/graphql/fragments/.keep, app/graphql/generated/.keep, graphql.config.yml, inserted `require "graph_weaver/rspec"` into spec/rails_helper.rb, introspected app/graphql/schema.json from the live Countries endpoint (29777 bytes). No .rubocop.yml existed so (correctly) nothing was touched there.
15:38 - Restructured by hand into the "Two remote APIs" multi-schema layout from docs/getting_started.md#more-than-one-schema: moved schema.json into app/graphql/countries/, made app/graphql/{countries,poke}/{queries,generated}, rewrote the initializer to declare COUNTRIES/POKE clients and two `GraphWeaver.graph` blocks. This part is copy-paste from the docs, not generator output — the docs explicitly show hand-editing here rather than a generator flag for "second graph."
15:38 - `rake graph_weaver:schema:refresh` fetched both dumps live: countries 29,777 bytes, poke 5,776,832 bytes (PokeAPI's Hasura-generated schema is enormous). Exit 0, both printed "refreshed ... from <url>".
15:39 - Wrote app/graphql/countries/queries/country.graphql (query($code: ID!) { country(code: $code) { name capital currency } }) and app/graphql/poke/queries/pokemon.graphql (query($id: Int!) { pokemon_v2_pokemon_by_pk(id: $id) { name height weight } }) — one variable each, per the brief. Peeked at poke/schema.json with a one-off python snippet to find a query root field with a scalar variable arg (pokemon_v2_pokemon_by_pk(id: Int!)) since PokeAPI's Hasura schema doesn't document itself outside the dump.
15:39-15:40 - Built minimal CountriesController/PokemonController + routes calling the generated modules, and spec/requests/{countries,pokemon}_spec.rb using `graphql: :fake` + `graphql_fake`. First run: countries spec green immediately; poke spec failed with
  `ArgumentError: a fake doesn't take PokemonV2PokemonByPk: — did you mean the pin "pokemon_v2_pokemon_by_pk"? ...`
  My mistake, not the gem's — I pinned by the generated Ruby struct name (PascalCase response-key struct) instead of the schema/response-key name the fake wants (snake_case, matching the query's selection key). Good error message: named the exact right key. Fixed the pin, both green.
  **First green fake spec: ~7 minutes from `rails new` (15:33) to green (15:40).**
15:40 - Live spec: spec/requests/countries_live_spec.rb, no graphql: :fake tag (just an ad hoc `graphql: :live` label — not a real gem tag, just metadata I made up to mark it in log/matrix), so it runs the real configured GraphWeaver client against the live Countries endpoint. Green, real data (Japan/Tokyo) round-tripped through the generated struct. Confirms the generated code + client works against the actual network, not just the fake.
15:40-15:41 - `rake graph_weaver:verify` -> "generated queries up to date" (exit 0). `rake graph_weaver:queries:check` -> "every query validates against the schema" (exit 0, live re-introspection of both endpoints). `rake graph_weaver:unused` -> correctly recognized both controllers' `render json: { ... }` lines as serializer excuses ("every prop counted as read — handed whole to a serializer at ... as `country`"/`pokemon`), 8 selections/0 unread across 2 queries, exit 0, and printed its own "lint not a proof" caveat verbatim as documented.
15:41 - Production boot: `RAILS_ENV=production SECRET_KEY_BASE=dummy bundle exec rails zeitwerk:check` -> "All is good!" (exit 0). Boot logged `transport: GraphWeaver::Transport::HTTP -> <url>` once per client (2 lines) and "loaded 2 generated module(s) from app/graphql/generated, app/graphql/*/generated" — no NameError, no Zeitwerk complaint about the two-graph directory layout under app/graphql/{countries,poke}/generated.
15:42 - Checked bare `require` paths brief asked about, outside bundler/Rails:
  - `require "graph_weaver/testing"` — fine standalone.
  - `require "graph_weaver/rspec"` — fine standalone.
  - `require "graph_weaver/log_subscriber"` — raises `NameError: uninitialized constant GraphWeaver::ActiveSupport` if required before `active_support/log_subscriber`. Not actually a broken promise: nothing in the README/docs tells you to require this file yourself — it's railtie-internal, and in a real Rails app it just works (verified below). Still, a bare `require` on a `lib/graph_weaver/*.rb` file failing on an undeclared external dependency is worth a note for 0.7.1: either an inline `require "active_support/log_subscriber"` at the top of the file, or leave it undocumented-for-direct-use (current state, arguably fine).
  - `require "graph_weaver/tasks"` — raises `NoMethodError: undefined method 'namespace' for main` if `rake` isn't required first. Expected and matches docs ("Add `require \"graph_weaver/tasks\"` to your Rakefile" — a Rakefile always has `namespace` from Rake already). Confirmed fine once `require "rake"` precedes it (15 graph_weaver tasks registered).
  - Full Rails app: confirmed via `rails runner` that `GraphWeaver::LogSubscriber` is auto-installed by the railtie and produces exactly the documented line: `GraphWeaver countries/CountryQuery (230.2ms) ok`.
15:43 - `ri GraphWeaver` / `ri RSpec` (any gemset-installed gem) both say "Nothing known about ..." — confirmed environment-wide, not graph_weaver-specific: `ri --list` only shows Base64/Bundler/RDoc (bundled-with-ruby docs), nothing from `~/.rvm/gems/ruby-3.4.9/doc/*` even though ri data exists there for many gems. This is rvm/`gem install --no-document`-by-default behavior, not a graph_weaver defect. Built docs explicitly with `gem rdoc graph_weaver --version 0.7.0` (succeeded, wrote doc/graph_weaver-0.7.0/ri/**) and pointed ri straight at it with `ri -d <path> GraphWeaver`: the rendered content is genuinely good — full class overview mirroring the README/getting-started framing, the three `rails g graph_weaver:install` argument forms, and the RSpec tag table (`:live` [default], `:fake`, `:in_process`, `:router`, `:wire`) matching testing.md verbatim. So the *source* comments are ri-worthy; only the default toolchain doesn't surface them, same as any other gem here. (Left the generated doc/graph_weaver-0.7.0/ri/ directory in place — a later `rm -rf` of it was blocked by the sandbox's auto-mode classifier; it's inert cache, not a gem-source change, so left as is.)
15:45 - Generator flags: `rails g graph_weaver:install --help` lists `--auth=AUTH` and `--schema`/`--no-schema`/`--skip-schema`, matching the README's flag table exactly. Spun up a throwaway copy of the app to test both together (`--auth=GITHUB_TOKEN --no-schema`, github.com endpoint): `--no-schema` correctly skipped the `introspect` step (no schema.json written, no network call), and the initializer correctly wrote `auth: ENV["GITHUB_TOKEN"]`. Both flags work as documented. Scratch copy deleted after.
15:47 - Peeked at graph_weaver.gemspec's `s.files` line (one line, packaging metadata, not lib/ or spec/ source) to confirm a suspicion from `gem contents`: `s.files = \`git ls-files * ':!:spec' ':!:sorbet' ':!:bin' ':!:examples' ...\`` — confirms **examples/ is deliberately excluded from the packaged gem**. The README promotes it prominently ("The **examples** run that path for real, smallest first...", "That's [`examples/federation.rb`](examples/federation.rb), the example that needs no network") but a user who only has the published gem (no git checkout) cannot run any of them — the files simply aren't there. The links resolve fine when reading the README *on GitHub* (relative links against the repo tree), so this is invisible unless you actually try to open the file locally.
15:47 - Full rspec run: 3 examples, 0 failures (countries fake + live, poke fake).
15:47 - Wrap-up.
