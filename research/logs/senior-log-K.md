# Senior K — the Apollo Router operator (production config)

08:02 - start. Read brief-senior-K.md + brief-senior4-common.md. Read senior-log-I.md in full (nearest: real gateway + real Rust router, but with `include_subgraph_errors.all=true` i.e. a DEV router config) and senior-log-C.md's findings (planner matrix, @interfaceObject routing-table bug, gateway data parity). Main at 49b9209 already carries the fixes for I's F1 (subgraph-origin stamp) and F4 (@defer refused by name) — so my angle is what I's dev-config router could not show: PRODUCTION router config. Persisted queries (APQ + safelist), header propagation + JWT auth, `include_subgraph_errors: false` redaction, entity caching + @cost limits, subscriptions/@defer multipart, traceparent correlation, and hot-reloaded supergraph drift.
08:03 - copied senior-app-C-fix -> senior-app-K, repointed Gemfile path + Gemfile.lock remote at the checkout.
08:04 - `bundle check` satisfied; baseline `rspec` 23/23 green. Reused senior I's already-downloaded Apollo Router v2.17.0 binary (prebuilt, no compile) rather than re-downloading. Wrote tmp/prod_harness.rb: 4 WEBrick subgraphs + a `boot_router(yaml)` that restarts the REAL router with an arbitrary production config, a real `GraphWeaver::Transport::HTTP` client pointed at it, and a raw HTTP escape hatch. Added one query file (overweight.graphql, `product(upc:) { name shippingEstimate }`) so an ERRORING path has a real generated module; `rake graph_weaver:generate` wrote 6 files clean.
08:06 - P1 (error redaction) done, 3 runs. include_subgraph_errors omitted (the production default) -> message becomes "Subgraph errors redacted" AND extensions is EMPTY — the `service` origin stamp main just added (senior I's F1, commit 49b9209) is present in the local :router and in a DEV-config real router, and absent in production. Failure.graphql CAN reproduce the redacted shape exactly. Also spotted in the router's own span: `client.name: ""`, `client.version: ""` — graph_weaver sends no apollographql-client-name/version headers (folded into P6).
08:08 - P2 (persisted queries), 3 runs. (a) Captured the wire body at the socket by subclassing Transport::HTTP#post: graph_weaver sends `{"query","variables","operationName"}` and NEVER `extensions.persistedQuery` — no APQ, no PQ id, and no hook to add one. (b) The router's APQ is live and free (hash-only -> PERSISTED_QUERY_NOT_FOUND, register, then hash-only works). (c) A ~12-line Transport::HTTP subclass overriding `post` bolts on full register-on-miss APQ and works cold and warm — the duck-typed door really is open. (d/e) `persisted_queries.safelist` REFUSED TO BOOT twice: first "apqs must be disabled to enable safelisting", then "license violation ... [\"Persisted queries\"]" — safelisting is GraphOS-licensed, so I cannot measure the safelist rejection verbatim on a self-hosted unlicensed router. Time-boxed; measured the adjacent real thing instead. (h) APQ hash mismatch gives a REAL router 400 with a GraphQL errors body, and graph_weaver's transport routes it into the envelope correctly: code PERSISTED_QUERY_HASH_MISMATCH preserved, no ServerError. (i) Built the manifest a team would need from the generated modules' own QUERY/OPERATION_NAME constants — 6 operations, 1840 bytes, ~10 lines of Ruby. Nothing in `rake -T graph_weaver` emits one.
08:12 - P3 (headers + auth) via a free rhai plugin (JWT auth, coprocessors, operation limits, demand control, subscriptions and persisted queries are ALL GraphOS-licensed and refuse to boot unlicensed; rhai and `headers:` are free). `headers.subgraphs.reviews.request.propagate.named: authorization` verified at the subgraph socket: reviews saw "Bearer good-token", accounts and products saw nil. A 403 with extensions round-trips perfectly into GraphQLError#code/#extensions. Retry does not retry it. A rotating credential works on the DEFAULT transport via a proc header (invoked per request, verified), but `auth:` refuses a Proc and its message sends you to Faraday middleware.
08:20 - P4 (statuses + retry). The find of the session: the router's own `traffic_shaping.router.global_rate_limit` answers HTTP **503** with `{"errors":[{"message":"Your request has been rate limited","extensions":{"code":"REQUEST_RATE_LIMITED"}}]}`. Because the body carries `errors`, Transport#perform returns the envelope instead of raising ServerError, so: Retry(retries: 3) made 1 attempt; Retry(retry_codes: THROTTLE_CODES) also made 1 attempt (REQUEST_RATE_LIMITED is not in THROTTLE_CODES); QueryError#throttled? => false. Same shape for a router-internal 503 and for the rhai-plugin 500. Verified against the real router, free features only.
08:26 - P5. A real subgraph timeout (traffic_shaping.all.timeout: 300ms, reviews stalled 1s) comes back 200 + GATEWAY_TIMEOUT + service:reviews, partial data preserved in the typed Result, 1 attempt through Retry. @defer is refused at GENERATION by graphql-ruby validation ("Directive @defer is not defined"), not by the planner's new by-name `incremental_delivery` category — the by-name refusal is in Testing::Router's planner, which a generated module never reaches. The router streams a real multipart body when Accept asks for it; graph_weaver's default Accept doesn't, so the router refuses first (DEFER_BAD_HEADER) — but forcing the header yields `ServerError: HTTP 200: non-GraphQL response: \r\n--graphql...`, a misdiagnosis. Subscriptions: refused at generation with "Schema is not configured for subscriptions" (the supergraph has no subscription root).
08:32 - P6/P7. traceparent works end to end: a proc header generates a distinct one per request, the router propagates the SAME trace id to every subgraph. Response side is one-way — no response headers on success, and `execute.graph_weaver` carries {url, operation, client, graph, status, http_status, duration_ms} with no trace/request id. Router span shows `client.name: ""`, `client.version: ""` — no apollographql-client-name/version headers. Drift: with the router hot-reloaded onto a supergraph missing Listing.condition, `verify`, `federation:diff` and `queries:check` ALL exited 0 while every ListingsQuery in production failed; only the runtime error was right (GRAPHQL_VALIDATION_FAILED, validation? true, schema_stale? true, message names schema:refresh && generate). `schema:diff` can't be pointed at the router: "supergraph.graphql records no source url — it wasn't introspected from one."
08:40 - P7d/e: `rake graph_weaver:schema:refresh` (no URL) aborts "supergraph.graphql records no source url — pass one: ... URL=https://api.example.com/graphql"; doing what it says against the router (introspection on) REPLACES the composed supergraph (5.8KB, 11 @join__graph directives) with the router's API schema (1672B, none), reports "refreshed supergraph.graphql from http://...", and afterwards federation:diff exits 1 while schema:diff/verify go green on the wrong artifact. Restored from a backup.
08:42 - P7f: `Testing::Router#execute` validates before it plans (testing/router.rb:314), so the planner's new by-name `incremental_delivery` refusal is still shadowed: a raw @defer query comes back as `{"message":"Directive @defer is not defined","extensions":{"code":"GRAPHQL_VALIDATION_FAILED"}}` — the same accident 49b9209 set out to stop depending on.
08:44 - The gem checkout moved under me mid-session (49b9209 -> 0df590a, five commits touching codegen/type_helpers/tasks/graph, none touching transport.rb, retry.rb, errors.rb, response.rb, testing/router.rb or schema_loader.rb). Re-ran the two headline probes against 0df590a: both reproduce verbatim. Final `bundle exec rspec` in the app: 23/23. Gem checkout left clean (never written to).
