# Queued for the final scribe pass (from senior D, scalars at the seams)

Code and the scalars.md sentences are with the structs lane (brief-structs-json.md). Left for the scribe:
1. docs/testing.md: fake-mode pins are keyed by schema scalar name, not Ruby class — two names for one class need two pins. One sentence beside the pin rule.
2. docs/testing.md `:wire` section: the senior hit the same around-vs-before ordering gotcha `Testing.configure` documents; check the `:wire` section says it, or link the one that does.
