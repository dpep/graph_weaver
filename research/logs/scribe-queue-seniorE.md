# Queued for the final scribe pass (from senior E, server-side input errors)

Source: /tmp/claude/graph_weaver/senior-log-E.md, app senior-app-E (spec/requests/i18n_spec.rb repros #1). No code defects; every item is prose. Verify each against the current docs before editing.

1. docs/errors.md and docs/i18n.md recipes call `e.field.underscore` — `#field` is nil for a `:refused` error with no stated path, which is the default experience against a server that has not adopted the convention (a plain `validates:` failure). Show the nil branch: `e.field ? form.errors.add(e.field.underscore, ...) : form.errors.add(:base, ...)`. The doc-samples spec allow-list may key on the old excerpt; update the key.
2. docs/errors.md Rails section: a JSON controller must deep-underscore incoming camelCase params before calling `execute`, or every key is `kind: :unknown` with "did you mean 'customer_email'?" — one sentence with the Rails one-liner (`params.deep_transform_keys(&:underscore)`), and say the hint is how you tell a casing problem from a typo.
3. docs/testing.md mode table: `:fake` never runs the server's `validates:`, custom validators or `@oneOf` enforcement — it fabricates a shape-correct success — so a `:fake`-only suite has zero coverage of server-side rejection. One sentence, pointing at `Failure.graphql(code:, extensions:)` and `:in_process` as the two ways to cover it.
4. docs/i18n.md: the recipe `I18n.t(kind, field: e.field, **details)` cannot say which list row failed; `#path` carries the index. Show one line that renders it (e.g. join the path for the label).
5. docs/logging.md: say plainly that `filter_parameters` scrubs what graph_weaver composes (InputError#message/#value, variables in log lines) and never a server-authored or resolver-authored `GraphQLError#message`; logging.rb's comment already says so, the doc reads like a blanket net.
6. docs/errors.md Hasura arm: a `where:` value wrong for the underlying column but accepted by Hasura's comparison type fails at Postgres as `data-exception`, which is not one of the two codes read — one sentence beside the Hasura row so the fallback is expected, since it is the most common real Hasura input mistake.
