# Senior H: federation directives the earlier passes never touched

Read /tmp/claude/graph_weaver/brief-senior2-common.md. Prior passes: senior C (`tail -c 20000` of /private/tmp/claude-501/-Users-dpepper-code-lib-ruby-graph-weaver/bb134e57-46b8-4797-a396-c7f5afad21be/tasks/ad89c14e307f5f1f7.output; log senior-log-C.md — the planner matrix, `@interfaceObject`, fetch count) and senior F (senior-log-F.md — cassettes, `@override` over time, `@provides`, context, lifecycle, mixed app). All findings fixed on main; verify in passing, don't re-derive. Start from a copy of /tmp/claude/graph_weaver/senior-app-F-fix as `senior-app-H`, Gemfile repointed. Log `senior-log-H.md`. docs/federation.md's refusal table lists what the router says it does and doesn't support; your job is the gap between that table and a current Apollo composition.

- **`@link` and renamed directives.** Compose a subgraph that imports federation directives under aliases (`@link(url: "https://specs.apollo.dev/federation/v2.9", import: [{name: "@key", as: "@primaryKey"}])`) and one that uses the unimported `@federation__requires` spelling. Does the routing table read the supergraph correctly regardless (the supergraph normalizes to `@join__*`, but check), and does `federation:diff` compare against the subgraph SDL correctly when the subgraph's SDL spells things differently.
- **Keys.** `@key(fields: "id", resolvable: false)` (a stub reference the subgraph can't resolve): does the router try to resolve there and fail, or plan around it? Multiple `@key`s on one type where the crossing subgraph has only the second key. A key over a nested object with a list inside. A type with `@key` in three subgraphs where two are identical stubs.
- **Progressive override**: `@override(from: "old", label: "percent(50)")` — what does the router do with a label it can't evaluate: refuse, ignore, or pick one side silently? Check the refusal table's claim.
- **`@context` / `@fromContext`** (federation 2.8): a field argument filled from an ancestor's field. The routing table has `contextual`; does the planner supply it, refuse it, or ignore it and send a null argument?
- **`@cost`, `@listSize`, `@requiresScopes`, `@authenticated`, `@policy`, `@tag`, `@inaccessible` on an enum value and on an argument**: each should be either transparent or refused by name. Find any that is silently mis-read (e.g. an `@inaccessible` argument still generated into the typed `execute`).
- **Abstract types across subgraphs.** An interface implemented in two subgraphs where only one implementation is an entity; a union whose members live in three subgraphs with one member `@inaccessible`; an interface field resolved through `@interfaceObject` now that the misattribution is fixed — the docs say `interface_object` refuses unconditionally, confirm the boundary is exactly the query that touches it.
- **Composition versions.** Compose the same subgraphs with federation 2.3, 2.6, and the latest `@apollo/composition`; diff the supergraphs; run every one of senior C's matrix queries against each. Any difference in outcome is a finding.
