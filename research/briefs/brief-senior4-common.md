# Senior round four — common rules

You are evaluating graph_weaver (gem checkout, read-only for you: /Users/dpepper/code/lib/ruby/graph_weaver, main at the sha in your launch message) for adoption, from the viewpoint your brief names. Six senior passes have already covered: registration doors, Money, equality (A); client-side input errors (B); the router's planner matrix and gateway data parity (C); scalars in inputs, modes, to_json, precision (D); server-side validation and i18n (E); cassettes, @override over time, @provides, context, lifecycle, mixed app (F); scalars across graphs and under srb tc (G); federation directives — @link aliases, keys, progressive override, @context, cost/auth directives, composition versions (H); error/edge parity against the JS gateway and the Rust router (I). Their logs are /tmp/claude/graph_weaver/senior-log-{A..I}.md; skim the "Ranked findings" of the two nearest yours so you don't repeat them. Everything they found is fixed on main or documented.

Toolchain: `~/.rvm/wrappers/ruby-3.4.9/bundle exec ...` (never `source rvm`). Build under `/tmp/claude/graph_weaver/<your app dir>` with a Gemfile `path:` at the checkout; prior apps (senior-app-C-fix has four real apollo-federation subgraphs and a composed supergraph; senior-app-G has two in-process graphs with Sorbet) may be copied and repointed. Do not modify the gem. Every claim in your report has a runnable repro; every message is quoted verbatim; every "the docs say X" is checked against what happened. Time-box dead ends at ~15 minutes; log and move on.

Keep a timestamped log at `/tmp/claude/graph_weaver/<your log>.md`. Final message: a matrix of what you drove × outcome; ranked findings (severity, repro, message verbatim, what a fix would look like, additive or not); a design critique paragraph from your viewpoint; the times you read lib/ or spec/ and why; minutes per step; one paragraph on whether you'd ship on this surface.
