# Brief: the last docs pass before the cut — juniors 4–10, the closure memo, the upgrade guide's shape

Repo: /Users/dpepper/code/lib/ruby/graph_weaver, main at the sha in your launch message (you are in a worktree; `git merge --ff-only main` first if behind with no commits). Read CLAUDE.md ("Docs are a complexity detector"). Toolchain `~/.rvm/wrappers/ruby-3.4.9/bundle exec ...`. Don't push. Verify every behavior claim against the current code or by running it; every "must" you write needs a reason you checked.

Sources: the junior reports (final messages of the task outputs under /private/tmp/claude-501/-Users-dpepper-code-lib-ruby-graph-weaver/bb134e57-46b8-4797-a396-c7f5afad21be/tasks/ — juniors 4: a9f767a1ddfe79691, 5: a736ecf7cd3ec5ae9, 6: ab28b4922e859827f, 7: a6bf5844aec3288c9, 8: a6dd8ea3d6b71d823, 9: afb1ac9cd1233f5b6, 10: aebff8514996f8e02; `tail -c 15000` each is enough) and the staff memo /tmp/claude/graph_weaver/inputs-memo.md. Code fixes from those reports are already merged; what's left is prose. Items, by file:

**docs/federation.md** (junior 6, the biggest gap of the round): a new subsection before "Generating against a supergraph" — *Producing a supergraph* — that says: your own subgraph's SDL comes from the `apollo-federation` gem (`Schema.federation_sdl`; name the gem in prose, it was only ever in the gemspec), other teams' subgraph SDL comes from them, and composition is Apollo's `rover supergraph compose` with a `supergraph-config.yaml` (show the four-line config and the one command; note the `--elv2-license accept` flag and that the repo's `examples/federation.rb` / `spec/support/federation/compose.mjs` show the `@apollo/composition` route for a node toolchain). Verify the rover command shape against Apollo's current docs (WebFetch) rather than the junior's memory. Then one sentence at the `:wire` mention that it needs `webmock/rspec` (link testing.md).

**docs/upgrading.md** (junior 8, all structure): split the lead sentence of "Upgrading from 0.6.1" into a checklist a reader can tick — one line per change with "applies if you …"; separate the grep hits that self-heal on regenerate from the ones to rename by hand (say which files the grep will find); list `rake graph_weaver:verify` as an explicit step at the end, not only in the intro; promote "a Rails app starts logging a line per call at info" out of the footnotes since it's a production log-volume change; give the "changed meaning without raising" items a one-line self-check each (`InputError#field`: "applies if you index a hash by `e.field`"; instrumentation `:status`: "applies if you subscribe to the event"). Keep the four version sections; add a one-line table of contents at the top so a reader lands in the right one.

**README.md** (the closure memo): the sentence claiming codegen is "query-driven, not the whole schema" is true for output selections and false for input types — an input object has no selection set, so its transitive closure is generated (Hasura-shaped schemas: one `where:` variable → ~1000 input types; hand-designed ones like GitHub: usually one). Correct it in one sentence and point at the escape hatch below. Also the `:fake` sample's faker line is already fixed — leave it.

**docs/generated_modules.md** (the memo's escape hatch): in the typed-variables/input-objects section, one short paragraph: a filter written as a literal in the query with a variable per leaf generates one file instead of the closure and gives `srb tc` concrete leaf types to check; a runtime-chosen sort column can't be inlined (GraphQL has no dynamic keys). The memo's before/after numbers (1183 files → 1 file for the same query) may be quoted, rounded. Also (junior 7) the `to_prepare` rule for `extend_type` — cross-link from where getting_started states the rule for scalars/enums to the extend_type paragraph here, one sentence each side.

**docs/errors.md** (juniors 5, 7): on Hasura-shaped schemas `#field` is the leaf comparison operator (`_gte`, `_in`), so key a form on `#path` (the second segment is usually the column) — one sentence beside the path table with the junior's `["where","height","_gte"]` example; and one sentence that `input_errors` is empty for a `validates:` failure unless the server adopts the convention (check testing.md's new sentence says the same; don't duplicate, link).

**docs/i18n.md** (junior 7): a short "Transport and server failures" paragraph — no `kind` there; key on the exception class (`TransportError`, `ServerError`, `QueryError` with no input errors), since `code` is a per-server invention and the class hierarchy is the one closed vocabulary; one static translated sentence per class. And a sentence that `details[:type]` is the GraphQL scalar name (`Int`), which a translated sentence will show in English unless the app maps it — show a two-line `types:` locale table.

**docs/testing.md** (junior 5): the README/testing claim "the tag is all you need" — check the intro after the earlier rewrite still oversells it; one sentence that a client-side `InputError` is raised before dispatch and so is independent of the mode (junior 7's confusion #9). Don't touch the `:wire` or failure sections (rewritten this round).

**DECISIONS.md**: a new entry recording the input-type closure decision from the memo — what was measured, why status quo, the escape hatch — in the file's existing shape.

Every fenced sample must parse (spec/doc_samples_spec.rb, which also resolves links); run behavior samples. One commit, trailers:

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01GbHLrRyUS8oTVYgKp5EMZo

Gate: `bundle exec rspec spec/doc_samples_spec.rb`, then the full `bundle exec rspec`, separate commands. Report: sha; per item done/verified/skipped-with-reason; anything false you found while verifying.

**Late additions from the lifecycle lane** (schema:diff/refresh now work per graph from a live class as well as a url): `README.md` ~158 says "`schema:diff` names what changed when the server has drifted" — the source is a class as often as a url now; reword. Same "the server" framing at `docs/generated_modules.md` ~121 and `docs/federation.md` ~170. And `docs/getting_started.md` §5 was rewritten by that lane (CI scripts per topology) — don't redo it; only check your other getting_started edits don't contradict it.
