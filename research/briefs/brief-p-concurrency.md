# Lane: senior P's concurrency findings — whoever owns the mutable field owns the lock

Read /tmp/claude/graph_weaver/brief-common.md for toolchain, gate, trailers. Baseline: main **df7043b** (worktree off it). 0.7.1 work: your bullets in one contiguous block under the existing `###  Unreleased` heading (other blocks there; keep all on rebase). Every gate command its own Bash call. Senior P's report is the final message of `/private/tmp/claude-501/-Users-dpepper-code-lib-ruby-graph-weaver/bb134e57-46b8-4797-a396-c7f5afad21be/tasks/a09f22f482699457c.output` (`tail -c 14000`), log `/tmp/claude/graph_weaver/senior-log-P.md`, repros under `/tmp/claude/graph_weaver/senior-app-P/` (`spec/wire_identity_spec.rb`, `script/endpoint_lock.rb`, four cassette spec files, `script/cache_clobber*.rb`, `script/cold_schema.rb`, `script/reqid_fork.rb`). Copy the app, repoint, run each before and after.

The reviewer's diagnosis is the brief: the lock got placed on the object the bug was found on rather than the object the state lives on, and three findings share one shape, a process-global resource with no process in charge. Fix the shape, not the instance; one rule per fix; if a rule needs an exception, say so and stop. **Another lane (writes) owns `transport.rb`, `retry.rb`, `query_module.rb`, `logging.rb`, `log_subscriber.rb`, `input_struct.rb`, `coerce.rb`, `Wire.check_variables!` and `testing/fake_client.rb` right now** — not yours; F6 touches `Wire.log_tag` in the same module as `check_variables!`, keep that edit to the tag method and expect a rebase.

**F1 (HIGH) — the Endpoint identity fix doesn't reach the harness path it was written for.** `rspec.rb` builds `Testing::Endpoint.new(client)` inside the `to_rack` lambda, per request, over a client memoized per example in `test_clients.rb`, so each request has its own Monitor over one shared `context=`. `wire_identity_spec.rb`: 6 of 8 crossed, five runs out of five. Rule: whoever owns the mutable field owns the lock. Put the lock on the object whose `context` is written (`InProcess`, `Router` — they define the accessors), or build the Endpoint once per graph in the closure — prefer the former, since it is one rule for every wrapper, and it makes F5 answerable in the same place. Spec: the reviewer's 8-thread `:wire` example, watched failing.

**F5 (MEDIUM) — the Monitor serializes clients with nothing to protect.** `with_context`'s early return asks `respond_to?(:context=)`, which `InProcess` and `Router` always answer yes, and the non-callable `next yield` sits inside the `synchronize`: a context hash costs 8× the baseline to guard a value nobody writes. Decide at construction from the context's callability (a proc needs the lock; a hash doesn't), not per request via `respond_to?` — the reviewer notes the naive "read outside the lock" is wrong for the reason the code comment gives. Measure before/after with `endpoint_lock.rb`; docs/testing.md's two sentences ("served one request at a time" / "served concurrently") must be true without qualification.

**F2 (MEDIUM-HIGH) — parallel processes recording one cassette lose 75% of entries, green.** `Cassette#initialize` snapshots the file once; `record` mutates the snapshot and `atomic_write`s the whole file; last renamer wins. Rule: a cassette is read-modify-written under an advisory `flock` on the file, re-reading inside the lock, so every process's entries survive. Spec with real subprocesses (fork 4, each records distinct keys, assert 20 survive), watched failing. Also the reviewer's "refuse to record under TEST_ENV_NUMBER" — not needed if the lock works; say so.

**F3 (MEDIUM) — `cache: true` is one file for every client in the process.** `SchemaLoader.cache_path` maps `true` to `GraphWeaver.schema_path` whatever the client's graph or url; two clients at two origins serve each other's schema, and concurrently it is a coin flip. junior-app-15 ships this shape. Rule: `cache: true` names a path derived from the client — its graph's dump path when it has a graph, else a file named by a digest of the url under the schema dir — so two clients never share a cache they didn't both name. Spec both clients; refuse nothing new unless the derivation is ambiguous.

**F4 (MEDIUM) — a cold `Client#schema` against a hung upstream serializes the worker: 8 threads × 30s each, linearly, no negative caching.** Memoize the failure briefly inside the lock (the first thread's `TransportError`, re-raised to the queued threads for a short window — say the window and why; a plain circuit breaker is more than needed). `cold_schema.rb`: serialization factor 8.0 → ~1. Spec with a never-answering server and a short timeout.

**F6 (LOW) — `[req N]` continues the parent's sequence in every forked worker.** Mix the pid in, or reset on first call in a new process the way the pool does; and put the same tag on the `LogSubscriber` line only if the writes lane's payload work makes that trivial (report otherwise). `reqid_fork.rb` before/after.

**Docs (yours):** docs/testing.md — the two concurrency sentences made true, cassettes under parallel processes; docs/getting_started.md or wherever `cache: true` is introduced — what path it names now; a short "what is process-global" paragraph in docs/logging.md or real_world.md listing the three resources and who owns each (the reviewer's table, condensed).

Ownership: `lib/graph_weaver/testing/endpoint.rb`, `lib/graph_weaver/in_process.rb`, `lib/graph_weaver/testing/router.rb` (the context accessor/lock only), `lib/graph_weaver/rspec.rb` (the `to_rack` closure only), `lib/graph_weaver/internal/test_clients.rb`, `lib/graph_weaver/testing/cassette.rb`, `lib/graph_weaver/schema_loader.rb` (`cache_path` only), `lib/graph_weaver/client.rb` (the schema lock only), `lib/graph_weaver/internal.rb` (`log_tag` only), `spec/endpoint_spec.rb`, `spec/wire_mode_spec.rb`, `spec/cassette_spec.rb`, `spec/client_spec.rb`, `spec/schema_loader_spec.rb`, `spec/thread_safety_spec.rb`, new specs, `docs/testing.md`, `docs/real_world.md`, `docs/getting_started.md` (the cache sentence), `CHANGELOG.md` (your block), `spec/support/public_surface.txt` via `bin/public-surface --update`.

Gate: brief-common's (rspec random seed, `srb tc`, `bin/generate` clean), two random seeds, `INTEGRATION=1 bundle exec rspec spec/integration` once, and every repro before/after pasted. Report: the rule per finding, the measurements, shas, `git status` clean.
