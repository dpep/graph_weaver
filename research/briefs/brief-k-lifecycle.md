# Lane: senior K's lifecycle and router findings — the refresh guard, @defer by name, redaction docs

Read /tmp/claude/graph_weaver/brief-common.md for toolchain, gate, trailers. Baseline: main **0df590a** (worktree off it). CHANGELOG: your bullets in one contiguous block under the existing `###  Unreleased` heading (other lane blocks are there; keep all on rebase). Every gate command its own Bash call. Senior K's report is the final message of `/private/tmp/claude-501/-Users-dpepper-code-lib-ruby-graph-weaver/bb134e57-46b8-4797-a396-c7f5afad21be/tasks/a84fc2a423a6548a8.output` (`tail -c 20000`), log `/tmp/claude/graph_weaver/senior-log-K.md`, app `/tmp/claude/graph_weaver/senior-app-K` (real Apollo Router, probes under `tmp/`). Copy to `-fix`, repoint the Gemfile, re-run `probe_p7` (drift and refresh) and the `@defer` probes before and after.

You own the lifecycle tasks and the router's refusal boundary. Standards: a task never destroys a checked-in artifact and reports success; a refusal the code deliberately makes must be reachable, not true by luck. If a fix needs a rule with an exception, say so and stop.

**K2 (HIGH) — `schema:refresh URL=<router>` overwrites a composed supergraph with the API schema and exits 0.** The path an operator walks: the runtime drift message says "refresh the schema dump and regenerate"; `schema:refresh` says "records no source url — pass one"; `schema:refresh URL=...` writes 1.6 KB of API schema over a 5.8 KB supergraph with eleven `@join__graph` directives, `federation:diff` then fails "no composed supergraph here" while `schema:diff` and `verify` go green against the wrong artifact. Refuse: when the dump on disk carries `@join__*` and the introspection result carries none, don't write — "this dump is a composed supergraph; introspection returns the API schema, not the routing table — recompose it (rover supergraph compose) instead". Make the "records no source url" message tell a federated app the same truth rather than suggest `URL=`. And the runtime drift message for a module whose graph's schema is a supergraph should say recompose, not refresh (find where `schema_stale?`'s advice is composed; it knows the graph). Specs on each message, watched failing; the app's `probe_p7` before/after.

**K7 (MEDIUM) — the by-name `@defer`/`@stream` refusal from 49b9209 is unreachable.** `Testing::Router#execute` validates before it plans (`errors = @planner.validate(document)` with an early return), so graphql-ruby's "Directive @defer is not defined" fires first, and `rake graph_weaver:generate` refuses with graphql-ruby's text too. Scan the document for `@defer`/`@stream` before validation, in the router's `execute` and at the codegen entry point, so the `incremental_delivery` message is what a user sees in both places. The existing spec composes a supergraph that declares the directives (the only way to reach the check today) — keep it, add the undeclared case, watch it fail.

**K4 (MEDIUM-HIGH, docs) — a `:router` suite asserts on what production redacts.** With `include_subgraph_errors` omitted (the production default) the router answers `{"message":"Subgraph errors redacted","path":[...]}` with `extensions` emptied — the message and the `extensions.service` stamp added at 49b9209 both vanish. `Failure.graphql("Subgraph errors redacted", path: [...])` reproduces the production shape. One paragraph in docs/testing.md (beside the router-mode section) and a cross-reference in docs/federation.md: production routers redact by default; assert on `path` and your own `extensions`, not on a subgraph's message or the `service` stamp; here is the `Failure` for it. Run the sample.

**K3 (HIGH, docs now, code later) — nothing in the CI toolbox looks at the schema production serves.** With the router hot-reloaded onto a drifted supergraph, `verify`, `federation:diff` and `queries:check` all exit 0; `schema:diff` can't be pointed at a supergraph and production routers refuse introspection. Say it out loud in docs/federation.md's CI section and docs/getting_started.md §5's federated topology: the checks compare the app to its own checked-in artifacts; the pre-deploy check against the live graph is `rover subgraph check` / `rover supergraph fetch` + recompose, with the two commands. Put "queries:check against a supergraph URL" on the post-release list in your report, not in code.

**K5 (docs sentence):** docs/transports.md or wherever the wire format is described — the client sends `{query, variables, operationName}` and no persisted-query id; a safelist with `require_id` will refuse it; the ten-line `Transport::HTTP` subclass in the senior's log is the workaround today. One sentence plus the subclass shape if it fits in five lines; the feature itself is post-release.

Ownership: `lib/graph_weaver/tasks.rb`, `lib/graph_weaver/internal/tasks.rb`, `lib/graph_weaver/schema_loader.rb` (refresh/introspect path only), `lib/graph_weaver.rb` (the drift advice, if it lives there), `lib/graph_weaver/query_module.rb` or wherever `schema_stale?`'s advice is composed, `lib/graph_weaver/testing/router.rb`, `lib/graph_weaver/internal/planner.rb`, `lib/graph_weaver/codegen.rb` (the entry-point scan only), `spec/rake_tasks_spec.rb`, `spec/schema_loader_spec.rb`, `spec/router_spec.rb`, `spec/codegen_spec.rb`, `spec/query_module_spec.rb`, new specs, `docs/federation.md`, `docs/testing.md`, `docs/getting_started.md` (§5 only), `docs/transports.md` (the one K5 sentence — coordinate: the transport lane owns the rest of that file; put yours in a clearly separate paragraph and expect a rebase), `CHANGELOG.md` (your block), `spec/support/public_surface.txt` via `bin/public-surface --update`. Not yours: `transport/**`, `retry.rb`, `errors.rb`, `client.rb`, `docs/errors.md`.

Gate: brief-common's (rspec random seed, `srb tc`, `bin/generate` clean), two random seeds, `bin/federation-diff`, `examples/federation.rb`, `INTEGRATION=1 bundle exec rspec spec/integration/router_parity_spec.rb`, and the app's `probe_p7` before/after pasted. Report: per finding with messages verbatim; shas; `git status` clean.
