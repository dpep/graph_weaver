# Lane: senior H's four federation bugs

Read /tmp/claude/graph_weaver/brief-common.md for toolchain, gate, trailers. Baseline: main **51abb6f** (worktree off it; the v0.7.0 tag sits there and will move onto your fixes — nothing is published). CHANGELOG lane marker: put your bullets in a new `###  Unreleased` heading above `###  v0.7.0  (2026-09-13)`, one contiguous block; the fold happens after you. Every gate command its own Bash call. The senior's report is the final message of `/private/tmp/claude-501/-Users-dpepper-code-lib-ruby-graph-weaver/bb134e57-46b8-4797-a396-c7f5afad21be/tasks/ac335e225836448e9.output` (`tail -c 14000`), log `/tmp/claude/graph_weaver/senior-log-H.md`, and every repro is a standalone script under `/tmp/claude/graph_weaver/senior-app-H/tmp/h/` (`context_check.rb`, `list_in_key.rb`, `progressive_override2.rb`, `link_alias_check.rb` with their supergraph_*.graphql). Copy the app to `senior-app-H-fix`, repoint the Gemfile, run each repro before and after.

You own the local router's promise: never a plausible wrong answer. Two of these four are exactly that, and both come from the same shape: the planner has a verbatim single-subgraph shortcut and a crossing-aware `plan_step`, and a check that lives in only one of them silently doesn't apply to the query the other path takes. Fix the shape once if you can — a check that must hold for every plan should run where every plan passes — and say in the report whether you did. If a fix needs a rule with an exception, say so and stop.

**#1 (CRITICAL) — `@fromContext` on the verbatim path sends `nil`.** `Internal::Planner#contextual!` runs only in `plan_step`; the comment above it ("a subtree handed to one subgraph whole carries its own context, which is why the verbatim path never reaches here") is the wrong assumption — a subgraph's resolver never fills a `@fromContext` argument, only a router injects it. Repro: `Product` and `Size` in one subgraph, `Size.price(countryCode:)` from context → resolver gets `country_code: nil`, answers 500 where a gateway answers 1000. Refuse it by the existing `context_argument` category wherever the routing table's `contextual` is non-empty for a reached field, on every path. Spec on an inline supergraph, watched failing; fix the comment.

**#3 (HIGH) — progressive `@override(label:)`: both sides stay resolvable and the router picks one silently, forever.** `RoutingTable::Field#override` is read nowhere. Read `overrideLabel` from `@join__field` and refuse the query by a new `Unplannable` category (name it beside `interface_object`/`context_argument` in `Testing::Router::CATEGORIES`, message says the label and that a local router can't evaluate a rollout percentage — test each side by composing without the label). Docs: a row in the refusal table.

**#2 (HIGH) — a `@key` selection containing a list.** `Codegen#key_params` types a nested composite key field as `T::Hash` regardless of list-ness; `Representation.dig` treats an Array as absent, so the correct shape is refused ("missing @key lineItems.sku") and a bare Hash is accepted and misrepresents the list as an object on the wire. Give `key_params` the list case (`T::Array[T::Hash[...]]` or the typed struct if that is what nested keys already use — match the existing nested-key spelling) and make `Representation.dig`/`assign` walk arrays. Specs: generated kwarg type, the correct shape accepted and serialized as a list, the bare hash refused naming the list. `bin/round-trip` and the federation fixtures must stay green.

**#4 (HIGH) — `@link(import: [{name: "@key", as: "@primaryKey"}])` crashes subgraph-SDL loading** with advice that blames the file. `SchemaLoader`'s `subgraph_namespace`/`add_subgraph_definitions`/`entity_names` handle a whole-spec `as:` but not a per-directive import alias; the `link_namespaces`/`imports` machinery that does exists on the supergraph path for `@inaccessible`. Reuse it on the subgraph path. Spec with the senior's `link_alias.graphql` shape inline; the unimported `@federation__key` spelling stays green.

**Docs (yours):** `docs/federation.md` — the refusal table rows for #1 (already there? make it say "on any path") and #3; the `@key`-with-a-list shape in the representations section; one sentence that `federation:diff` compares live Ruby classes and never reads a subgraph's SDL spelling (senior's #6); one sentence that `@apollo/federation-internals`' `Supergraph.build` throws on `@authenticated`/`@requiresScopes`/`@policy` while `composeServices` succeeds, so `spec/support/federation/compose.mjs`'s pattern needs the raw compose for those (senior's #7) — or fix compose.mjs if it is a few lines.

Ownership: `lib/graph_weaver/internal/planner.rb`, `lib/graph_weaver/testing/router.rb`, `lib/graph_weaver/representation.rb`, `lib/graph_weaver/codegen.rb` (`key_params` and the representation emit only), `lib/graph_weaver/schema_loader.rb` (subgraph-SDL path and RoutingTable), `spec/support/federation/**`, `spec/router_spec.rb`, `spec/routing_table_spec.rb`, `spec/representation_spec.rb`, `spec/schema_loader_spec.rb`, `spec/federation*_spec.rb`, `spec/codegen_spec.rb` (key sections), new specs, `spec/generated/**` via `bin/generate`, `docs/federation.md`, `CHANGELOG.md` (your block), `spec/support/public_surface.txt` via `bin/public-surface --update`. Not yours: everything else. Two read-only seniors are running against main; nothing else writes.

Gate: brief-common's (rspec random seed, `srb tc`, `bin/generate` clean), two random seeds, `bin/federation-diff`, `examples/federation.rb`, `INTEGRATION=1 bundle exec rspec spec/integration/router_parity_spec.rb`, `bin/round-trip -c 2000`, and every one of the senior's repro scripts run against your worktree with the before/after output pasted. Report: per finding with messages verbatim and whether the one-shape fix landed; shas; `git status` clean.
