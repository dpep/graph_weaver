# Brief: the fold before the cut — CHANGELOG, upgrading.md, and three queued doc lists

Repo /Users/dpepper/code/lib/ruby/graph_weaver, main at **9a3d00d** (you are in a worktree; nothing else is running). Read CLAUDE.md ("Docs are a complexity detector"). Toolchain `~/.rvm/wrappers/ruby-3.4.9/bundle exec ...`. Don't push, don't touch version.rb or the tag. Verify every behavior claim you write against the code or by running it.

**1. Fold.** `CHANGELOG.md` has a `###  Unreleased` heading holding fourteen `<!-- lane: … -->` blocks (hints, structs, fedF, provenance, fedC, transport2, corpus, pool, wire2, junior4, lifecycle, seniorB, multigraph, seniorA) above `###  v0.7.0  (2026-09-12)`. 0.7.0 has not been published, so every block folds into the one v0.7.0 entry, which is written as the diff from 0.6.1. Rules: change the date to 2026-09-13; a fix to a surface that did not exist at 0.6.1 (check with `git show v0.6.1:lib/graph_weaver/<file>` or `git log v0.6.1..main -- <file>`) is a bug that never reached anyone, so it disappears or becomes a clause of the bullet that introduced the surface — the per-graph mode collapse, the `isOneOf` fallback regression, the pin/option casing rule, the `:wire` rules are examples to check; a fix to a 0.6.1 surface (`to_json` on results, the shape-drift blame, the type-string `InputError`, the `@interfaceObject` misattribution, the dump-provenance credential, transport failures naming no url, the `@oneOf` introspection loss, the input-type-named-after-a-constant refusal) stays as its own bullet. Merge bullets that say the same thing twice from different lanes; keep every *Action:* a user must take, and put those first under a short "What you must do" run at the top of the entry — the rest grouped the way the entry already groups. Remove the lane comments and the empty `###  Unreleased` heading (v0.6.1's cut had none — `git show v0.6.1:CHANGELOG.md | head`). The entry will be long; cut words, not facts.

**2. Reconcile `docs/upgrading.md` "Upgrading from 0.6.1"** with the folded entry: every *Action* in the changelog has a checklist line there with "applies if you …", and nothing there describes behavior the code no longer has (the file was last reconciled several lanes ago — diff its claims against the current messages by running them where cheap).

**3. Three queued doc lists**, each item verified before editing: `/tmp/claude/graph_weaver/scribe-queue-nosorbet.md` (README and getting_started say static Sorbet is optional; a generated header comment), `/tmp/claude/graph_weaver/scribe-queue-seniorE.md` (errors.md/i18n.md recipes with a nil `field`, camelCase params, `:fake` never runs server validators, logging.md's filter scope, the Hasura Postgres arm), `/tmp/claude/graph_weaver/scribe-queue-seniorD.md` (two testing.md sentences). For the generated header comment, the source is `lib/graph_weaver/codegen/emit.rb`; if you change it, `bundle exec ruby bin/generate` must be run and the fixture diff committed with it.

**4. `DECISIONS.md`**: one entry for the result-JSON rule ("a result's JSON is the wire shape; `to_h` is the Ruby view" — the reasoning is in commit fbcac73's message) and one for the per-graph mode rule (commit a3e0176), in the file's existing shape. Skip either if a lane already wrote it.

Every fenced sample must parse and links must resolve (`spec/doc_samples_spec.rb`; its allow-list keys on literal excerpts — update the key when you reword). Commits: the fold as one commit, the docs as one or two, trailers on each:

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01GbHLrRyUS8oTVYgKp5EMZo

Gate, separate commands: `bundle exec rspec spec/doc_samples_spec.rb`, then the full `bundle exec rspec`, `bundle exec srb tc`, `bundle exec ruby bin/generate` clean. Report: shas; the bullet count before and after the fold and which fixes were dropped as never-shipped, each with the evidence; per queued item done/verified/skipped-with-reason; anything false you found while verifying.
