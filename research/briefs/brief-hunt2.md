# Brief: hunt 2 — the surfaces that landed since hunt 1

Read /tmp/claude/graph_weaver/brief-common.md for toolchain (`~/.rvm/wrappers/ruby-3.4.9/bundle exec ...`, never `source rvm`); baseline is main **4730ec0**, the re-cut v0.7.0 tag (1514 examples, surface 475, CI green on Ruby 3.3/3.4/4, parity 73/2/0). Read CLAUDE.md, DECISIONS.md, the CHANGELOG `v0.7.0` entry (it is now the whole diff from 0.6.1), and hunt 1's report at `/tmp/claude/graph_weaver/hunt-report.md` so you don't re-find what's fixed — every A/B/C item there is closed; the "Hunches" section has three still open (non-atomic `||=` init, the round-trip `legacy_invalid` query, and nothing else). Read-only in the repo except scratch specs you delete before finishing; commit nothing; no git commands that change state. Scratch: /tmp/claude/graph_weaver/hunt2-*.

## Who you are

The skeptic, again. Hunt 1's findings all came from surfaces built in the previous round; the surfaces built since are where hunt 2's are. Rank by harm: silent wrong answer > crash > refusal that misdirects > paper cut. Repro or it's a hunch.

## Where to dig, in order

1. **`InputError` kinds, path, coordinate** (`lib/graph_weaver/errors.rb`, `coerce.rb`, `input_struct.rb`, `internal/refusal.rb`, `internal/redact.rb`; `spec/input_errors_spec.rb` is the map). Does `path` stay correct through every layer: a list of input objects with a bad element at index 3, a list of lists, an input object inside a list inside an input object, `nil` list elements, a `@oneOf` input, a variable whose default is an input object? Does `coordinate` ever lie (the report says nil for four cases — find a fifth where it's *wrong* rather than nil)? Does `kind` ever misclassify (a String for a Float that is `"1e400"`, a Hash for an enum, an Integer for an ID which is legal, a `TimeWithZone`-alike for a `Date`)? Server side: feed `GraphQLError#input_errors` every shape graphql-ruby 2.6 actually emits (run a scratch schema with `validates:` rules, custom scalar `coerce_input` raising, `@oneOf`, missing required nested fields, `argumentNotAccepted`, unknown enum) plus Apollo Router / Federation error shapes (`BAD_USER_INPUT` with and without `extensions.input`, `GRAPHQL_VALIDATION_FAILED`), and an `extensions.input` with a hostile `kind`, a `path` that isn't an array, a `value` that's 10 MB. Redaction: does a filtered key survive scrubbing when it's a Symbol, nested under an index, or inside `details`? Does `to_h` round-trip through `JSON.generate`?
2. **`ResultStruct`** (`lib/graph_weaver/result_struct.rb`, the emitted `include`). `==` between a struct and a Hash, a struct and a struct of a different class with the same props, subclass vs parent; `hash` stability across processes; `deconstruct_keys(nil)`; pattern matching with a union member vs the catch-all `Other`; `to_h` on a struct holding a `T::Enum`, a `BigDecimal`, a `Time` with subsec, a nested list of unions; a result key that collides with `eql?`/`hash`/`==` (are those in `STRUCT_METHODS`?); `Marshal`/`YAML` of a struct; `inspect` cost on a 10k-node result.
3. **Renames and the redaction change**: any remaining `TypeError`/`ValidationError` reachable by a user (grep generated fixtures, examples, the install generator's templates, error `to_h["error"]` strings, i18n keys); did the message scrub change any message a spec doesn't pin (diff the messages `bin/round-trip --hostile` produces on 4730ec0 vs the v0.6.1 tag for a sample of refusals)?
4. **Railtie and `to_prepare`** (`lib/graph_weaver/railtie.rb`, `spec/railtie_spec.rb`): the refusal for a late output under a Zeitwerk root — does it fire for an output that's a *symlink* into a root, a relative path, a `Pathname`, `Rails.root.join`? Does `config.graph_weaver`'s refusal break `config.graph_weaver.watch = false` in any Rails version's OrderedOptions (`respond_to?`, `to_h`, `fetch`, `key?`, `dig`)? The throwaway app is `/tmp/claude/graph_weaver/dogfood-rails-railtie` (repoint its Gemfile `path:` at the main checkout).
5. **`GraphWeaver.configure`**, the generator's rubocop append (an existing `.rubocop.yml` with `inherit_from`, with `AllCops:` nested under a `---` document marker, with CRLF), `Transport::HTTP` callable headers under the fork-safe pool (is the proc called per request or per connection?), `Faraday#url` with `params` that aren't strings, `ServerError#headers` with a header that appears twice.
6. **The three open hunches**: prove or clear each with a repro; the `legacy_invalid` one is worth isolating (`bundle exec ruby bin/round-trip -c 2000 2>&1 | grep -B2 legacy_invalid`).
7. **Property harness, widened**: `bin/round-trip -c 5000`, `--hostile -c 3000`, `examples/github/schema.json -c 1500`, plus the 600-schema fuzzer from hunt 1 if its script is still under `/tmp/claude/graph_weaver/hunt-fuzz/` — re-run it on 4730ec0; the emitter changed (ResultStruct include, sixth FIELDS column, splice fix).

## Report

`/tmp/claude/graph_weaver/hunt2-report.md` and the same text in your final message. Ranked, with harm class, repro (file + command + exact output), one-sentence fix, files touched (for partitioning). Hunch list separate. Then what you tried that turned up nothing.
