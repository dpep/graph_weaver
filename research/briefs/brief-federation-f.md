# Lane: senior F follow-ups — per-graph modes, type-aware drift, the second-graph refusal

Read /tmp/claude/graph_weaver/brief-common.md for toolchain, gate, trailers. Baseline: main **71abf1e** (worktree off it). CHANGELOG lane marker `<!-- lane: fedF -->` at the top of `###  Unreleased`. Every gate command its own Bash call. Senior F's report is the final message of `/private/tmp/claude-501/-Users-dpepper-code-lib-ruby-graph-weaver/bb134e57-46b8-4797-a396-c7f5afad21be/tasks/ab3b01b293f8785c9.output` (`tail -c 16000`), log `/tmp/claude/graph_weaver/senior-log-F.md`, app `/tmp/claude/graph_weaver/senior-app-F` (copy to `-fix`, repoint Gemfile at your worktree; its 42 specs are your reality test, and the repros for #1 and #3 are in it).

You own the test-mode state and the federation drift check. The project's standard: a behavior you can state in one sentence, no silent success where the tool could have told, and "don't answer a varying question with a global setting". Both findings are places a simple boolean can't see its own blind spot. If a fix needs a rule with an exception, say so and stop.

**#1 (MEDIUM) — two helpers, two graphs, one example: the mode collapses to whichever was named last.** `graphql_router(graph: :store)` then `graphql_fake(graph: :countries, ...)`: `Internal::TestClients#install` resets `@clients = {}` when `@mode` changes, wiping the first helper's per-graph stand-in. Reversed order raises the `:router`-plans-across-a-supergraph refusal for the plain graph. `:wire` already does the right thing per graph. Read `install`, `override!`, `standin` and the comment describing intended behavior (the senior says the code doesn't match it). Two candidates: (a) refuse a second helper in a different mode the way a tag/helper mismatch is already refused, naming both calls and pointing at `:wire`; (b) make the stand-in per graph — the tag sets the example's default, a `graph:` helper sets that graph's stand-in, and no helper ever resets another graph's. (b) is the sentence the docs already imply and it is what `:wire` does; prefer it if `install` can be reshaped without a second bookkeeping path, otherwise (a). Whichever you pick, the docs/testing.md `graph:` paragraph must say what the rule is. Specs: both orders from the senior's repro, watched failing; the existing tag/helper conflict specs still green.

**#3 (MEDIUM) — `federation:diff` is blind to a field's type.** `Internal::Schemas.defines?` is `type.fields.key?(field_name)`. Redefining `Warehouse.code` from `String!` to `ID!` in the loaded class with the supergraph untouched reports "matches". Compare the type signature too (`field.type.to_type_signature` against the supergraph's field type — mind that a supergraph prints federation-rewritten types the same way the subgraph SDL does; check with `bin/federation-diff` on the fixture and with the senior's app). Report it as its own drift kind ("shape" beside "stale"/"not composed in") with the two signatures in the message. Also the `@override` asymmetry: code ahead of the supergraph (new side declares the field with `@override` before recompose) is silent while supergraph-ahead is caught — see whether the same comparison closes it; if it can't without reading `@override` from the class (apollo-federation exposes it on the field definition), do that only if it is a few lines, else one sentence in docs/federation.md.

**#2 (MEDIUM, message + docs) — declaring a second graph makes `generate` refuse the pre-existing default graph's files.** The refusal is correct (hunt 3's A2) but not discoverable: the message names the files and says "under directories no declared graph covers", and neither doc says that naming graph two means naming graph one. Make the message say the fix in one sentence when the orphaned directory is the default `schema_path`/queries dir ("wrap the default in a named graph too: GraphWeaver.graph :store do ... end — no namespace: needed to keep the constant names"), and add the sentence to getting_started.md's multi-graph section and federation.md where it says a single-schema app's output "says nothing about graphs".

**Docs only, in your files:** docs/federation.md — `orphan_types` also needed for an extend-only type unreachable from that subgraph's Query (second variant of the ordering footgun, silent); the stale/not-composed checks are coordinate-presence until #3 lands, say what they are after it; "one subgraph times out while another answers" has no representation under `:router` or `:wire` (subgraphs are calls, not sockets), one sentence in the limitations; a raw non-GraphQL exception from a subgraph resolver propagates as a Ruby exception, same as graphql-ruby alone — leave the code, say it beside the errors-crossing-the-seam paragraph.

Ownership: `lib/graph_weaver/internal/test_clients.rb`, `lib/graph_weaver/rspec.rb`, `lib/graph_weaver/testing/**` except `router.rb`'s planner (read it, don't reshape), `lib/graph_weaver/federation.rb`, `lib/graph_weaver/internal/schemas.rb`, `lib/graph_weaver/internal/tasks.rb` and `lib/graph_weaver/tasks.rb` (the orphan refusal message), `spec/rspec_spec.rb`, `spec/testing_spec.rb`, `spec/federation_drift_spec.rb`, `spec/rake_tasks_spec.rb`, `spec/generate_spec.rb`, new specs, `docs/testing.md`, `docs/federation.md`, `docs/getting_started.md` (the multi-graph section only — another lane is editing one sentence elsewhere in that file; rebase), `CHANGELOG.md` (your block), `spec/support/public_surface.txt` via `bin/public-surface --update`. Not yours: `result_struct.rb`, `input_struct.rb`, `codegen/**`, `schema_loader.rb`, `docs/generated_modules.md`, `docs/scalars.md`. A mutation-testing lane is adding specs across `spec/**`; expect to rebase.

Gate: brief-common's (rspec random seed, `srb tc`, `bin/generate` clean), two random seeds, `bin/federation-diff`, `examples/federation.rb`, `INTEGRATION=1 bundle exec rspec spec/integration/router_parity_spec.rb`, and the senior's app specs green. Report: the mode rule in one sentence and which candidate you chose and why; per finding with messages verbatim; shas; `git status` clean.
