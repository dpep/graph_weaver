# Lane: result JSON and the type-string input error (senior D)

Read /tmp/claude/graph_weaver/brief-common.md for toolchain, gate, trailers. Baseline: main **71abf1e** (worktree off it). CHANGELOG lane marker `<!-- lane: structs -->` as a new block at the top of `###  Unreleased`. Every gate command its own Bash call. Senior D's report is the final message of `/private/tmp/claude-501/-Users-dpepper-code-lib-ruby-graph-weaver/bb134e57-46b8-4797-a396-c7f5afad21be/tasks/a7d45e3cff6806ea5.output` (`tail -c 12000`), log `/tmp/claude/graph_weaver/senior-log-D.md`, app `/tmp/claude/graph_weaver/senior-app-D` (copy to `-fix`, repoint Gemfile at your worktree; its 29 specs are your reality test).

You own the two generated-struct mixins. The standard: one rule a user can predict without reading the source, and no silent wrong answer. If a fix below needs a rule with an exception, say so and stop.

**1 (HIGH) — `ResultStruct#to_json` is Ruby's default: the inspect string, quoted.** Confirmed on main:
```
k = Class.new(T::Struct) { include GraphWeaver::ResultStruct; const :name, String; const :price, Float }
k.new(name: "x", price: 1.5).to_json  # => "\"#<#<Class:0x…>:0x…>\""
```
Check what Rails' `Object#as_json` gives for the same struct (`instance_values`, probably the ivars, also wrong). Decide the rule and state it in the docstring. The candidates: (a) `to_json` is `to_h.to_json` — Ruby prop names, each leaf's own `#to_json`, which the senior showed diverges from the registered wire form (`Money#to_json` → `"12,50"`); (b) `as_json`/`to_json` render the wire form — the response keys and each scalar through its registered `serialize:` — so that `from_h(JSON.parse(x.to_json))` gives back an equal struct. I lean (b): "a result's JSON is what the server sent for this selection, and `from_h` reads it back" is one sentence, it is round-trippable, and it is what a cache or a log line wants. But check what `InputStruct#to_h`/`serialize` already do (wire keys, `serialize:` applied) and keep the two struct kinds consistent: if an input's `to_h` is wire-shaped and a result's `to_h` is Ruby-shaped, don't add a third shape — say which JSON each produces and why they agree or differ, in `docs/generated_modules.md`'s serialization paragraph. Define `as_json` too (Rails calls it, and `render json:` goes through it). Spec: a struct with a registered object scalar (Money), a nested struct, a list, an enum, a nilable nil, through `to_json` → `JSON.parse` → `from_h` → `==`. Watch it fail first. Update getting_started.md's coverage-lint sentence that lists `to_json`/`as_json` as reads, if the rule changes what it should say.

**2 (MEDIUM) — a type-string registration (`register_scalar("Vector", "T::Array[Float]")`) gives a worse `InputError`.** `[1, 2, 3]` for a `T::Array[Float]` field: `T::Utils.coerce(...).valid?` is shallow and says yes, the setter's recursive check raises, and `InputStruct#mistyped` (read it) then lands the coordinate on the containing list, empty `details`, and Sorbet's raw wording with the generated module name in the message. Fix so a type-string field reports like a class-registered one: the field's own coordinate, `details[:type]`, no Sorbet spelling. Recursing into `T::Array`/`T::Hash`/`T.nilable` element types in `mistyped` is the likely shape; keep it to the types the emitter can produce (check `codegen/scalar_type.rb` for the type strings it accepts). Spec on the senior's exact case plus a nested `T::Array[T::Array[Float]]` and a `T.nilable(T::Array[Float])`; watch each fail first.

**3 (docs, in your files)** — `docs/scalars.md`: the `URI::Generic` recipe raises `URI must be ascii only` on a unicode literal; one sentence and the escape (`URI::DEFAULT_PARSER.escape` or `Addressable`). And one sentence for scalar authors: graphql-ruby calls a nullable scalar's `coerce_input` with `nil` on an explicit `null`, so a naive coercer crashes there and `:in_process` will show it as a `ServerError`.

Ownership: `lib/graph_weaver/result_struct.rb`, `lib/graph_weaver/input_struct.rb`, `lib/graph_weaver/codegen/emit.rb` only if the rule for 1 needs the emitter to hand the struct its wire keys (say so in the report), `spec/result_struct_spec.rb`, `spec/input_struct_spec.rb`, `spec/custom_scalar_spec.rb`, new specs, `spec/generated/**` via `bin/generate`, `docs/generated_modules.md` (serialization paragraph), `docs/scalars.md`, `docs/getting_started.md` (the one sentence), `CHANGELOG.md` (your block), `spec/support/public_surface.txt` via `bin/public-surface --update`. Not yours: everything else; a mutation-testing lane is adding specs across `spec/**` right now and has been told to stay out of your two lib files — expect to rebase your spec files onto its additions.

Gate: brief-common's (rspec random seed, `srb tc`, `bin/generate` clean), two random seeds, `bin/round-trip -c 2000`, the cached pokeapi/countries/github dumps chunked by seed (`-c 100 -s 1`, `-s 101`), and the senior's app specs green. Report: the JSON rule in one sentence and what Rails gave before; per finding with messages verbatim; shas; `git status` clean.
