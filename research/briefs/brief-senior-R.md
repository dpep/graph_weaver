# Senior R: the checkout engineer (mutations, the write path)

Read /tmp/claude/graph_weaver/brief-round6-common.md. App dir `senior-app-R`, log `senior-log-R.md`. Every senior so far read data; you own a checkout flow, and your queries are mutations with money on the line. You care about exactly-once, idempotency, partial failure, and what the client does when the answer is ambiguous.

- Build an in-process graphql-ruby schema with a `placeOrder` mutation (input object with line items, a `Money` scalar, an idempotency key argument), a `payment` mutation that can partially fail (order created, charge declined — `userErrors` payload AND a top-level error variant), a mutation returning a union payload (`OrderPayload | ValidationErrors`), a mutation with a nested mutation-ish side effect, and one that takes an `Upload` (know that it's refused now, drive it anyway for the message).
- **Exactly-once**: `Retry` around a mutation on a transport failure (never retried — confirm at the server with a counter), on a 503 with an errors body (the new status rule — does `retry_mutations` still govern it?), on a stale keep-alive socket, on a `Retry-After` 429; `retry_mutations: true` with an idempotency key — what the docs say to do and whether it works.
- **Ambiguity**: a mutation that returns `data` AND `errors` (partial success): what `execute!` raises, what `execute` returns, whether the created order id is reachable from the exception; `data: null` with errors; a transport failure after the server committed (simulate with a server that commits then closes the socket) — is there any hook to reconcile.
- **Input side**: an `InputError` on the third line item's `Money` — path/coordinate/details; a `@oneOf` payment method; a variable given as a `T::Struct` vs a Hash vs keyword args (all three spellings the docs mention); `serialize:` on a `BigDecimal` amount (precision through JSON; "12.5" vs 12.5); the generated input struct's `to_h` used as a form's params round trip.
- **Test harness for writes**: `graphql: :fake` for a mutation (what does the fake fabricate for a payload union? for `userErrors`?), a cassette recording a mutation and replaying it (is a replayed mutation a lie the docs warn about?), `Failure.graphql(code: "CARD_DECLINED", extensions: ...)`, and `Sequence` for "declined then accepted".
- **The log line and the APM payload for a mutation**: does it say mutation, does the operation name reach the subscriber, is the variable (with a card number in it) anywhere.
Report the matrix, the exactly-once results with server-side counters, and every place the client was quiet where a payments engineer needed it to speak.
