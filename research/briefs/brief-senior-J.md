# Senior J: the performance engineer on scalars and federation

Read /tmp/claude/graph_weaver/brief-senior4-common.md. App dir `senior-app-J`, log `senior-log-J.md`. You care about where the time and memory actually go, measured on a quiet machine with a stated budget, and you distrust the interesting optimization — the expensive thing is usually structural.

- **Scalar casting on the hot path.** A query returning 10k rows × 8 leaves, four of them registered scalars (Date, DateTime, BigDecimal, a Money object scalar) and one a mapped enum, through `from_h`: wall time and allocations (`benchmark-ips`, `memory_profiler`) per row, vs the same JSON through plain `JSON.parse`. Where does the time go — `Coerce`, the sorbet-runtime prop setters, the `T::Struct` construction, the enum lookup? Then with sorbet-runtime's checks off (`T::Configuration.default_checked_level = :never`) to size what the runtime checks cost. Then the same 10k rows serialized back out with `to_json` (new this release). State the per-row cost to the precision it has.
- **The fake.** `graphql_fake` fabricating a 1k-row list with nested objects and registered scalars: time per example, and whether it scales linearly in `list_size:`; a suite of 200 tagged examples — is there setup cost per example (schema parse, registry read, supergraph parse) that should be memoized once? The user asked earlier whether supergraph parsing is memoized; measure `Testing::Router.new` on a wide supergraph called 200 times.
- **The planner on a wide supergraph.** Compose a 12-subgraph supergraph (script it: N subgraphs each with an entity keyed on `id`, each extending two others) and plan senior C's dashboard-shaped query: plan time vs subgraph count, and vs selection depth. Then `RoutingTable` construction on GitLab-scale SDL (the corpus dumps under /tmp/claude/graph_weaver/corpus/ may include a large supergraph; if not, generate one).
- **Generation.** `rake graph_weaver:generate` on the PokeAPI dump (cached under $TMPDIR by the integration specs, or `bin/round-trip` fetches it) with one `where:` variable — the input-type closure the staff memo measured at ~1000 files: wall time, peak RSS, and `verify` time on the result; is `verify` proportional to what changed or to the whole tree.
- **Transport.** The bundled HTTP pool under 32 threads against a local server: connection reuse rate, and whether `pool_size:` changes anything measurable.
Report numbers in a table, each with what backs it, rounded to what the measurement supports; name the one structural cost you'd fix first and what you'd expect it to buy.
