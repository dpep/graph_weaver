# Lane: host and graph — hunt 3's A1, A2, A3, E2, plus tasks paper cuts

Read /tmp/claude/graph_weaver/brief-hunt2-common.md for the lane rules (same shape; baseline is now main **34970d0**; the report is `/tmp/claude/graph_weaver/hunt3-report.md` — read A1, A2, A3, E2 and the tasks paper cuts in full; repro scripts under `/tmp/claude/graph_weaver/hunt3_*.rb`). CHANGELOG lane marker `<!-- lane: host3 -->` under `###  Unreleased`.

You are the production engineer who owns the Rails seam and the graph declaration. Four of hunt 3's five publish-blockers are yours, and every one is "reports success while not working".

- **A1**: a bare graphql-ruby schema class in a graph's `client` slot (the docs' own `client "Billing::Schema"`) is executed directly and has no instrumentation seam — zero events, zero log lines, not even at debug, while the sibling `client "GITHUB"` graph is fully instrumented. `GraphWeaver.new(Schema)` wraps it in `InProcess`, which instruments. Fix: `Graph#client` (and `GraphWeaver.client=`, whose docstring allows a schema class) wrap a bare schema class the way `GraphWeaver.new` does, so every documented spelling instruments. Repro: `hunt3_bareschema.rb` → events must be 1 for both. Then `rake graph_weaver:graphs` prints each graph's client class so an unwrapped one is visible. Specs for both entry points.
- **A2**: declaring a graph orphans the default `app/graphql/queries` directory: a `.graphql` file there with a typo'd field is skipped by `generate!`, and `queries:check` says "every query validates" about it. Refuse at generation (and in `queries:check`) when `.graphql` files or generated output sit under a directory no declared graph covers, naming the files and the graphs. Repro: `hunt3_orphan.rb`.
- **A3**: `GraphWeaver.logger = nil` / `instrumenter = nil` in a config/initializers file are reverted by the railtie: `graph_weaver.logger` runs *after* `load_config_initializers` (TSort puts it there because `ignore_generated` declares `after:` that), so the `if nil?` fallback wins and the documented PII opt-out does nothing. The railtie's own comment says the opposite. Fix: order those two initializers `before: :load_config_initializers` (verify with `Rails.application.initializers.tsort.map(&:name)` in the throwaway app), or use a sentinel for "explicitly off". Prove it in `/tmp/claude/graph_weaver/dogfood-rails-railtie` (copy to `-host3`, repoint Gemfile at your worktree): an initializer sets both to nil; after boot both read nil; a request logs nothing. Correct the railtie comment and docs/logging.md's claims.
- **E2**: ActiveSupport isn't in the bundle, so the railtie and `LogSubscriber` are tested against a stand-in that cannot model Rails' initializer TSort — which is exactly A3's mechanism. Add `activesupport` (and `railties` if needed for a real `Rails::Application` boot) as development dependencies, and give `spec/railtie_spec.rb` ONE spec that boots a minimal real Rails application in-process (a `Class.new(Rails::Application)` with `config.eager_load = false`, initializers run) and asserts the A3 ordering and the auto-wire. Keep the stand-in specs. If a real boot can't run inside the suite (say why with the error), the fallback is a subprocess spec that boots the throwaway app — slower but honest.
- **Tasks paper cuts**: `STRICT=0` turns strict ON (`tasks.rb` ~199 — parse as a boolean); `unused` with zero `.graphql` files should say `no queries in <dir>` like its sibling; "1 files swept" pluralization. (The `unused` accusations themselves — A4 — are the other lane's, in `internal/unused.rb`.)

Ownership: `lib/graph_weaver.rb`, `lib/graph_weaver/graph.rb`, `lib/graph_weaver/client.rb`, `lib/graph_weaver/railtie.rb`, `lib/graph_weaver/tasks.rb`, `lib/graph_weaver/internal/tasks.rb`, `graph_weaver.gemspec` + `Gemfile.lock` (another agent may add `rack` there concurrently — rebase and keep both), `spec/railtie_spec.rb`, `spec/graphs_spec.rb`, `spec/rake_tasks_spec.rb`, `spec/generate_spec.rb`, `spec/client_spec.rb`, new specs, `docs/logging.md` (the claims A3 falsifies), `docs/getting_started.md` (only the multi-graph example's client line if A1 changes what it should show), `CHANGELOG.md` (your block). Not yours: codegen/**, hints.rb, errors.rb, rspec.rb, testing/**, in_process.rb, internal/unused.rb, generators, spec/support/round_trip.rb.

Gate: brief-common's, two random seeds, plus the throwaway app boots pasted. Report: per finding fixed/refused/left with messages; the TSort order after the fix; the real-boot spec's approach; shas; `git status` clean.
