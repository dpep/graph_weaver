# Senior Q: the version-matrix engineer

Read /tmp/claude/graph_weaver/brief-round6-common.md. App dir `senior-app-Q`, log `senior-log-Q.md`. The `@oneOf` bug lived for a whole release because one graphql-ruby version was ever exercised; CI runs Ruby 3.3/3.4/4 but one graphql-ruby (the lock's), one Rails (whatever the throwaway app resolved, 7.2 or 8.1), one Sorbet. Your job is to run the suite and a throwaway app across the versions the gemspec claims to support, and report every difference.

- **graphql-ruby**: the gemspec floor (`>= 2.6.7`), the lock's version, and the latest 2.x on RubyGems (check). For each: `bundle exec rspec` with the gem pinned via `BUNDLE_GEMFILE` on a copied Gemfile, `srb tc`, `bin/generate` clean, `bin/round-trip -c 500`, `bin/federation-diff`, and the integration specs. Every failure is a finding with the version pair; a deprecation warning that will become an error (graphql-ruby prints "will return an error in future GraphQL-Ruby versions" for some field-merging shapes) is a finding too, with the count.
- **Rails**: 7.1, 7.2, 8.0, 8.1 (whatever installs; note which) — a throwaway app per version with the install generator, two graphs, generate, the rspec helper, a `:fake` and a `:wire` request spec, `verify`, production boot + `zeitwerk:check`, and the railtie's initializer ordering (`Rails.application.initializers.tsort.map(&:name)` around `graph_weaver.*`). The railtie was fixed against one Rails's TSort; check the others.
- **Sorbet**: the lock's `sorbet-runtime` and the latest; `sorbet-static` latest against the generated fixtures (`srb tc` in a scratch project that includes `spec/generated/**`); Tapioca's `dsl`/`gem` against the gem (does Tapioca need an RBI compiler for generated structs, and does `tapioca gem graph_weaver` produce a usable RBI?).
- **Ruby**: 3.3.2 is installed locally (`~/.rvm/wrappers/ruby-3.3.2/`); run the suite there once too, three seeds, since CI showed a 3.3-only socket race last week.
- **Faraday**: the optional transport with Faraday 1.x (if the gemspec allows it) and 2.x.
Report a version × outcome matrix, each cell green/red/warnings-with-count, the exact failing examples and messages per red cell, and a recommendation for which versions CI should pin as matrix dimensions (with the cost in minutes per dimension measured from your runs).
