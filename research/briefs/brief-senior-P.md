# Senior P: the concurrency reviewer

Read /tmp/claude/graph_weaver/brief-round6-common.md. App dir `senior-app-P`, log `senior-log-P.md`. The security review found `Testing::Endpoint` serving one request's identity to another under eight threads, one commit after a "thread-safety audit" — which suggests that audit stopped at the request path. You assume every piece of shared state is a race until shown otherwise, and you prove it with threads, forks and parallel processes, not by reading.

- **Under Puma**: a real Rails app (copy junior-app-15, repoint) booted under Puma with 8 threads and 2 workers, `preload_app!`, hammered with 200 concurrent requests that each execute a generated query against a raw local server (spec/support/raw_http_server.rb shows one; or WEBrick) — the pool (`pool_size:`), keep-alive reuse after fork, `Retry` with concurrent attempts, the `execute.graph_weaver` payload (does any field cross requests), `Internal::Log.with_graph`'s fiber-local under Puma's threads, the introspection cache write when 8 threads cold-start at once (the client has a lock; the file write?), the schema `||=` under threads.
- **The registry and reload**: `GraphWeaver.register_scalar`/`extend_type` from `to_prepare` while requests are in flight; `Rails.application.reloader.reload!` in one thread while another executes; a graph declared twice concurrently.
- **The test harness under parallel_tests / turbo_tests**: 4 processes each running a `graphql: :fake` suite with `seed:`, a `:wire` suite (WebMock is process-global — does one process's stub see another's? they are processes, so no, but what about `Testing.configure` and the class-level config), a cassette suite where two processes record the same key at once (the known lost-update: measure it and say what the file looks like after), `Testing::Router` built per example vs memoized (`built_router`) under threads within one process (rspec is single-threaded, but a `Concurrent::Future` inside an example isn't).
- **Class-level state grep**: every `@@`, `class << self; attr_accessor`, `Mutex`, `Monitor`, `Thread.current`, `Fiber[]`, `||=` on a module in `lib/` — a table: what it holds, who writes it, when, and whether the write is guarded or benign-racy. That table is the deliverable even where nothing breaks.
- **Fork safety again**: the pool after `fork` without `preload_app!` (Puma cluster mode, and Spring), the cassette file handle, the introspection cache handle, any `Thread` the gem starts (the railtie watcher?).
Report the state table, the races reproduced with thread counts and iteration counts to the precision they have, and what a fix would look like for each.
