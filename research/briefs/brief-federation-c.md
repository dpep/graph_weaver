# Lane: federation follow-ups from senior pass C

Read /tmp/claude/graph_weaver/brief-common.md for toolchain, gate, trailers. Baseline: main **4fd16d2** (you are in a worktree off it). CHANGELOG lane marker `<!-- lane: fedC -->` as a new block under `###  Unreleased`, above the other lane blocks. Every gate command its own Bash call.

You are the engineer who owns the local router's promise: it never answers a query production would answer differently, and it refuses loudly and specifically everywhere else. A silent wrong attribution is the one failure this surface is not allowed to have. If you conclude a change below makes the rule longer to state than the bug it fixes, say so and stop.

The senior's full report is the final message in `/private/tmp/claude-501/-Users-dpepper-code-lib-ruby-graph-weaver/bb134e57-46b8-4797-a396-c7f5afad21be/tasks/ad89c14e307f5f1f7.output` (`tail -c 20000`), log at `/tmp/claude/graph_weaver/senior-log-C.md`, working app at `/tmp/claude/graph_weaver/senior-app-C` (four apollo-federation subgraphs, composed `supergraph.graphql`, specs under `spec/federation_*_spec.rb`). Copy the app to `senior-app-C-fix` and repoint its Gemfile at your worktree before touching it; it is your reality test.

**#1 (HIGH, the one that matters) — a bare `@join__field` on an `@interfaceObject`'s implementer is attributed to the concrete type's home graph.** `SchemaLoader::RoutingTable#read_fields` keeps a `@join__field` with no `graph:` as a `Field` with empty `graphs` and empty `external`; `#owners` then falls through to `declared_in(type_name)`. Apollo's composer emits exactly that bare directive on a concrete implementer's copy of a field that is really contributed through `@interfaceObject` elsewhere. Consequence, verified by the senior two ways in the app: pinning `"products" => Products::Schema` raises `ConfigurationError` at `Router.new`; with auto-detection `products` is reported absent and `{ products { name price } }` — nothing to do with the interface — is refused `absent_subgraph` with advice ("probably just not loaded yet") that is wrong. Repro from the senior: `table.owners("Bundle", "reviews")` returns `["products"]` for a field `Products::Schema` never defines. First write a routing-table spec on an inline supergraph SDL carrying that shape and watch it fail; then fix. Decide the rule yourself: the candidates are to attribute such fields through the existing `interface_objects` table, or to exclude them from the "must define" contract the router checks at construction. Pick the one you can state in a sentence, and say it in the `owners` docstring. Then confirm in the app: pinned and auto-detected router both construct, the unrelated products query runs, and the `@interfaceObject` query is still refused `interface_object`. Check `bin/federation-diff` and the fixture supergraph still agree.

**#3 (MEDIUM-HIGH) — a loaded subgraph class that matches no subgraph in the supergraph is invisible to `federation:diff`.** Every `federation:*` task iterates the routing table's subgraph list, so retiring a subgraph from the composition while its Ruby schema stays loaded reports "matches the schemas here (checked 3 of 3 subgraphs)", exit 0. Add the mirror check: federated schema classes loaded in the process (the same detection auto-detect uses) that fit no subgraph name are reported as a distinct section and fail the task, naming the class and the subgraphs that exist. Keep it one rule; if auto-detection can't tell "retired" from "a federated schema this app happens to load for another supergraph", say what the honest message is and prefer a warning that names the ambiguity over a false failure. Spec it in the rake spec.

**#2 (MEDIUM) — fetch count is higher than a real gateway's for the same query.** Dashboard query: real gateway 4 fetches, local router 6, byte-identical data. The router batches rows within a fetch but issues one fetch per selection purpose (a plain external read vs a `@requires` prefetch) even when both target the same subgraph at the same node. `router.trace` is documented as an N+1 regression tool, so this is a cost-faithfulness gap. Two deliverables, in order: (a) docs/federation.md says plainly that the trace's fetch count is the local router's plan, not the gateway's, and that a regression assertion should be on a *bound* or on the subgraph *set*, not an exact count; (b) then look at whether merging sibling field-groups bound for the same subgraph, same node, same level is a small change in the planner. If it is small and `INTEGRATION=1 bundle exec rspec spec/integration/router_parity_spec.rb` stays green, do it and drop (a)'s caveat to what remains true; if it isn't small, leave (a) and report why. Do not reshape the planner for this.

**#4 and #6 (docs only)** — in docs/federation.md: one sentence that a new `@join__` directive anywhere in a shared supergraph refuses `Router.new` for the whole graph, so adopting a newer federation feature in one team's subgraph is an upgrade-timing event for every team that tests with `:router`; and one sentence beside the refusal table that a document failing ordinary GraphQL validation (undefined fragment, a subscription against a schema with no Subscription root) never reaches the router's refusals, it gets the same `errors` response a plain client gets.

**#5** — apollo-federation gem footguns (`orphan_types` before `query`, Zeitwerk sibling references). Not ours; one sentence in the "Producing a supergraph" section of docs/federation.md for the `orphan_types` ordering, since it drops types from the printed SDL silently and a reader following our docs will hit it. Skip the Zeitwerk one.

Ownership: `lib/graph_weaver/schema_loader.rb` (RoutingTable only), `lib/graph_weaver/testing/router.rb`, `lib/graph_weaver/testing/**`, `lib/graph_weaver/tasks.rb` and `lib/graph_weaver/internal/tasks.rb` (federation tasks only), `bin/federation-diff`, `spec/support/federation/**`, `spec/schema_loader_spec.rb`, `spec/router_spec.rb` and other router/federation specs, `spec/rake_tasks_spec.rb` (federation describes only), `spec/integration/router_parity_spec.rb`, `docs/federation.md`, `CHANGELOG.md` (your block). Not yours: codegen/**, rspec.rb, graph.rb, errors.rb, client.rb, other docs. Nobody else is running; rebase is not expected but check `git log main -1` before reporting.

Gate: brief-common's (rspec random seed, `srb tc`, `bin/generate` clean), two random seeds, `bin/federation-diff`, `examples/federation.rb`, the parity spec with `INTEGRATION=1`, and the senior's app specs green against your worktree. Report: per finding fixed/refused/left with the rule you chose and the message text verbatim; the fetch count before/after for the dashboard query; shas; `git status` clean.
