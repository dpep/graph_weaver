# Brief: the fold before the cut, second time — five lanes into v0.7.0

Repo /Users/dpepper/code/lib/ruby/graph_weaver, main at **c2aacc4** (you are in a worktree; nothing else is running). Read CLAUDE.md ("Docs are a complexity detector"). Toolchain `~/.rvm/wrappers/ruby-3.4.9/bundle exec ...`. Don't push, don't touch version.rb or the tag. Verify every behavior claim against the code or by running it. The previous fold's brief is /tmp/claude/graph_weaver/brief-scribe-fold2.md and its rules apply unchanged; this is the same job over the lanes that landed since.

**1. Fold.** `CHANGELOG.md` lines 1–194 are a `###  Unreleased` heading with five lane blocks (federation H: check_reachable!/@fromContext/@override(label:)/@defer/list keys/@link aliases/extensions.service; scalars G: type-helper naming, pins accepting objects, per-graph attribution; J: list_size Hash; K transport: retry rule, client-name headers, auth: proc, multipart message, File refusal; K lifecycle: refresh guard, @defer reachability, SUPERGRAPH= refusal, docs) above `###  v0.7.0  (2026-09-13)`. 0.7.0 is still unpublished, so every block folds into that one entry, dated the day you commit (check `date`). Same rules: a fix to a surface that did not exist at 0.6.1 disappears or becomes a clause of the bullet that introduced the surface (`git show v0.6.1:lib/graph_weaver/<file>` is the evidence — check especially: the extend_type-in-a-graph-block naming (graph.rb is new), progressive @override and @fromContext refusals (were they refused at all at 0.6.1? `testing/router.rb`'s CATEGORIES then), list_size Hash (the fake existed; list_size did?), the refresh guard (schema:refresh existed at 0.6.1?), SUPERGRAPH= refusal (the env var existed?)); a fix to a 0.6.1 surface stays its own bullet (retry inert behind a router, File in a variable, the @link alias crash, list-in-key, auth: proc, multipart message are likely 0.6.1 surfaces — verify). Two *Actions* must reach the "What you must do" run: a spec comparing a `Testing::Router` error hash whole now sees `extensions.service` (the H lane's changelog says so), and a Faraday transport now resolves a callable header per request instead of refusing it (the K transport lane deleted that spec). Merge duplicates across lanes, keep every *Action*, remove the lane comments and the empty `###  Unreleased` heading. Cut words, not facts.

**2. Reconcile `docs/upgrading.md` "Upgrading from 0.6.1"** with the folded entry: every *Action* has a checklist row with "applies if you …"; nothing there describes behavior the code no longer has. Check the greps it names still find what they claim.

**3. `DECISIONS.md`**: entries for the retry rule ("a retryable status retries whether or not a body came with it", commit 240d3c2's message has the reasoning), the planner's `check_reachable!` shape (9695be0), the type-helper naming rule (1ddfa98), and the `SUPERGRAPH=` refusal over honoring (cfa9015). Skip any a lane already wrote. The file's existing shape.

**4. One docs read-through for contradictions**, not a rewrite: `docs/federation.md` grew four sections this round from three lanes (refusal table rows, "In CI", contracts, the role signpost, redaction cross-refs) — read it top to bottom once for a sentence that now contradicts another or repeats one, and fix only those. Same for `docs/testing.md` (three lanes edited it) and `docs/transports.md` (two).

Every fenced sample must parse and links must resolve (`spec/doc_samples_spec.rb`; update its allow-list keys when you reword). Commits: the fold as one, docs as one or two, trailers on each:

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01GbHLrRyUS8oTVYgKp5EMZo

Gate, separate commands: `bundle exec rspec spec/doc_samples_spec.rb`, then the full `bundle exec rspec`, `bundle exec srb tc`, `bundle exec ruby bin/generate` clean. Report: shas; bullet count before and after; which fixes were dropped as never-shipped, each with the evidence; the contradictions found in step 4; anything false you found while verifying.
