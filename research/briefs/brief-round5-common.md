# Round five — common rules

You are evaluating graph_weaver 0.7.0 **as published on RubyGems** (`gem "graph_weaver", "0.7.0"` in a Gemfile, NOT `path:`), which no earlier pass has done — fourteen seniors and fourteen juniors all used a path checkout. The source is at /Users/dpepper/code/lib/ruby/graph_weaver (main a92bfd4, tag v0.7.0 = 50a2bce), read-only for you; the earlier logs are /tmp/claude/graph_weaver/senior-log-{A..M}.md and the post-release list is /tmp/claude/graph_weaver/followups-post-070.md — don't re-report what is there. Toolchain: `~/.rvm/wrappers/ruby-3.4.9/bundle exec ...` (never `source rvm`). Build under `/tmp/claude/graph_weaver/<your app dir>`, unique name. Do not modify the gem. Every claim has a runnable repro, every message is quoted verbatim, every "the docs say X" checked. Time-box dead ends at ~15 minutes. Anything you find is 0.7.1 material: report it, don't fix it.

Keep a timestamped log at `/tmp/claude/graph_weaver/<your log>.md`. Final message: a matrix of what you drove × outcome; ranked findings (severity, repro, message verbatim, what a fix would look like); a design critique from your viewpoint; the times you read lib/ or spec/ and why; minutes per step; one paragraph on whether you'd ship on this surface.
