# Lane: codegen and harness — hunt 3's E1, A4, C1, C3, C4, and the harness paper cuts

Read /tmp/claude/graph_weaver/brief-hunt2-common.md for the lane rules (same shape; baseline is now main **34970d0**; the report is `/tmp/claude/graph_weaver/hunt3-report.md` — read E1, A4, C1, C3, C4, the harness section and the paper cuts in full; repro scripts under `/tmp/claude/graph_weaver/hunt3_*.rb`). CHANGELOG lane marker `<!-- lane: codegen3 -->` under `###  Unreleased`. Every gate command its own Bash call; for `bin/round-trip` on the PokeAPI dump, chunk by seed (`-c 100 -s 1`, then `-s 101`) — the tool is superlinear in `-c` and `-c 1000` was killed at 6 GB RSS.

You are the staff engineer who owns generation and the property harness.

- **E1 (the one that matters)**: the round-trip harness uses `Inflect.underscore` at six sites in `spec/support/round_trip.rb` (~448, 606, 647, 700, 743, 826) instead of the prop rule, so on any schema with a reserved field name it reports thousands of *false* failures — and the value-loss check at 647 is exactly the net that cannot see a renamed prop. So v0.7.0's headline rename has no property coverage and the mandated gate goes red on the first real schema that exercises it. Expose the prop rule once (`Codegen.prop_name` or `Inflect.prop_name` — one public-or-internal home; `Response#report` in errors.rb will call it too, another agent is doing that) and use it at all six sites. Then add a fixture schema with reserved names to `spec/round_trip_spec.rb`'s set (an inline SDL with `class`, `hash`, `display`, `to_json`, `each`, plus an input type with `class`), so the rename is under property coverage for good. Repro: `bin/round-trip /tmp/claude/graph_weaver/hunt3-*/reserved.graphql -c 100` (find the file; the report names it) → 0 failures after.
- **A4**: `rake graph_weaver:unused` accuses fields you use, three ways: (a) a typo'd `PATHS=` sweeps nothing and accuses everything — `Dir.exist?` each root and refuse naming it; (b) the serializer excuse is line-scoped, so `result = X.execute!; render json: result.person` on two lines accuses four fields — when a file holds both the module name and a sink, suppress with a stated reason instead of accusing (the footer already admits the class of miss; a per-field accusation is louder than the footer); (c) `SCHEMA` skips whole `GraphQL::Schema::Resolver`/`Mutation` files, which hold app logic — narrow it to type kinds (`Object|Interface|Union|Enum|Scalar|InputObject`), and note graphql-ruby's generator emits `< Types::BaseObject`, which the skip doesn't match (docs/getting_started ~333 claims otherwise — fix the claim or the match). Also: print the wire coordinate (`Person.firstName`) beside the prop when they differ; `.rake` and `.builder` are Ruby. E3's measured 50–65% unreportable rate on real corpora goes into the docs as one honest sentence beside the worked example.
- **C1**: `check_input_props!`'s advice names a type the user never declared a variable of, fires when the query never sets the colliding field, and for a collision reachable only through a list has no writable form. Refuse only when the query can reach the colliding field; name the variable the user actually declared; when no literal form exists, say so.
- **C3**: shape drift (server sends an object where a list belongs) is branded with the parent struct, never names the field, and reports a false `got T::Array[String]` because `Hash#map` turned the object into pairs. Beside `Hints.cast_message`'s existing precedent, name the field and the actual wire type; spec the three shapes in `hunt3_shape.rb`.
- **C4**: a federation `@key` on a reserved field (`class`) is refused with no way out — a subgraph's key isn't yours to rename — and `@key` on `hash` emits `def self.slot(hash:)` beside `const :hash_`, so writing back is a bare `ArgumentError`. Apply the prop rule to key kwargs (`class_:`), and say so in docs/federation.md. `INTEGRATION=1 bundle exec rspec spec/integration/router_parity_spec.rb` if router.rb is touched.
- **Harness paper cuts**: the `refused` counter conflates codegen refusals with "no spoilable leaf" cases in hostile mode — count them apart so the summary doesn't blame codegen for 19% of the budget; `round_trip.rb` ~196 embeds a literal NUL that makes `rg`/`grep` treat the file as binary — spell it `"\0"`; a registered custom scalar's input refusal leaks `Date.iso8601`'s "no implicit conversion of Integer into String" where "expected a Date or an ISO-8601 String, got Integer" is the sentence (`codegen/scalar_type.rb` or `coerce.rb` — whichever brands it; you own scalar_type.rb, not coerce.rb: report if it's there). Add "chunk by seed for large schemas" to CLAUDE.md's round-trip paragraph, one sentence.
- **D4** (doc claim): CHANGELOG/generated_modules/upgrading say `#to_h` and pattern matching use the renamed prop "in results and input types alike" — `InputStruct#to_h` is aliased to `serialize` (wire keys) and input structs have no `deconstruct_keys`. Fix the claims in `docs/generated_modules.md` and `docs/upgrading.md` to what's true (CHANGELOG is folded later — put the corrected wording in your lane block so the fold picks it up).

Ownership: `lib/graph_weaver/codegen.rb`, `lib/graph_weaver/codegen/**`, `lib/graph_weaver/inflect.rb`, `lib/graph_weaver/hints.rb`, `lib/graph_weaver/internal/unused.rb`, `lib/graph_weaver/federation.rb`, `spec/support/round_trip.rb`, `bin/round-trip`, `spec/round_trip_spec.rb`, `spec/codegen_spec.rb`, `spec/hints_spec.rb`, the unused examples in `spec/rake_tasks_spec.rb` (another lane edits that file for STRICT parsing — keep your edits to the `unused` describe block; rebase), `spec/generated/**` via `bin/generate`, new specs, `docs/generated_modules.md`, `docs/federation.md`, `docs/upgrading.md`, `docs/getting_started.md` (the `unused` section only), `CLAUDE.md` (one sentence), `CHANGELOG.md` (your block), `spec/support/public_surface.txt`. Not yours: errors.rb, graph.rb, railtie.rb, tasks.rb, rspec.rb, testing/**, in_process.rb, generators, coerce.rb.

Gate: brief-common's including `bin/round-trip -c 2000`, `--hostile -c 1000`, the reserved-name fixture, and the dump sweep chunked by seed; two random seeds. Report: per finding; the prop-rule home; the reserved-name round-trip result; shas; `git status` clean.
