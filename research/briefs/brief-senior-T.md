# Senior T: the subgraph author

Read /tmp/claude/graph_weaver/brief-round6-common.md. Copy /tmp/claude/graph_weaver/senior-app-C-fix to `senior-app-T` (four real apollo-federation subgraphs, composed supergraph), repoint. Log `senior-log-T.md`. Every federation pass so far was a CLIENT of the supergraph (C, F, H, I, K, L). docs/federation.md's signpost names a third reader: the app that IS a subgraph — it serves `_entities` for types other subgraphs extend, and it may also call the supergraph. Nobody has driven that role end to end.

- **Serving entities with graph_weaver's help.** The docs describe typed representations (`Representation`, the `@key` kwargs `key_params` emits, `lineItems[]` list hops from H's fix), `federation:subgraphs`, `federation:coverage`. As the reviews team: your subgraph extends `Product` (owned by products) and `User` (owned by accounts) with `@key`s of every shape the supergraph has (scalar, composite, nested object, the list one). Write the `_entities` resolver using what the gem emits; drive it from the local router, from `@apollo/gateway` (the harness scripts), and from a raw `_entities` POST with representations you build by hand — including a representation missing a key field, one with an extra field, one with the wrong `__typename`, one whose key is the wrong type, and a batch of 500. What does the typed side refuse, what does it accept and misbuild, what does graphql-ruby do with the rest.
- **Both roles in one app.** Your subgraph also calls the supergraph (a `GraphWeaver.graph :platform` with the composed supergraph as its schema) to resolve a field that needs data from products. Under `:router` in specs: the router runs your subgraph's resolvers in-process AND your resolvers call the router — is that a cycle, does it plan, what does the trace show; under `:wire`; under `:in_process` for your own graph. The `unplaced`/`shape` drift checks when your own subgraph is one of the loaded classes AND a client.
- **The subgraph's own evolution.** Add a `@key`, remove one, change `resolvable:`, add `@requires` on a field, add `@shareable` to something another subgraph owns — for each: `federation:diff` before recompose, `rover subgraph check`-shaped composition, what the client-side generated code sees after. `federation:coverage` at each step — what does it measure and is the number honest.
- **What the gem emits for a subgraph vs what apollo-federation-ruby emits.** `Schema.federation_sdl` vs `GraphWeaver::SchemaLoader.load` on that SDL vs the composed supergraph's view of your subgraph: three spellings of one type; which does each task compare and does any pair disagree in a way that matters (directive spellings, `@link` header, `_Entity` union membership, `_Service`).
Report the matrix, what the typed representation layer caught vs let through, and what a subgraph team would need that isn't there.
