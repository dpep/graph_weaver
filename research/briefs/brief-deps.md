# Lane: merge the open Dependabot PRs and clear the security alerts

Repo /Users/dpepper/code/lib/ruby/graph_weaver on GitHub as dpep/graph_weaver; you work on the main checkout directly (no worktree; nothing else writes to the repo while you run, three read-only agents use the published gem). Toolchain `~/.rvm/wrappers/ruby-3.4.9/bundle exec ...` (never `source rvm`); use `git -C`, `npm --prefix`, `gh -R` rather than `cd`. Any GitHub comment you post starts with "claudomatic:". Trailers on every commit you make:

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01GbHLrRyUS8oTVYgKp5EMZo

**Context.** GitHub shows 17 open Dependabot security alerts, all in `spec/support/federation/package-lock.json` — the npm harness that runs a real @apollo/gateway for the parity spec; nothing in the shipped gem. `npm audit` there: tar (critical), make-fetch-happen/cacache (high), qs, uuid, @opentelemetry/core and its sdk packages, @apollo/server ≤5.4.0 (moderate). Its "fix available" for the @apollo/* packages proposes DOWNGRADES to 2.3.6/2.13.4 — do not take those; `@apollo/*` stays `^2.14.4` (an earlier gateway/composition version split broke parity, and 2.14.4 is what the whole session's federation work was measured against). Open PRs: #43 (Dependabot, tar + make-fetch-happen in the harness, checks green), #40 (actions/setup-node 4→7, a check failing), #18 (yard 0.9.44→0.9.45, a check failing).

**Do, in order:**
1. For each PR, `gh pr checks`/`gh pr view` to see which check fails and why (read the failing job log). A stale branch against a moved main (Gemfile.lock has changed a great deal since #18 opened; CI runs a frozen bundle install and fails at setup with exit 16 on a stale lock) wants `@dependabot rebase` as a comment, then wait for the new run (`gh pr checks --watch`). A failure that is the bump itself (setup-node 7 dropping something the workflow uses) wants a fix on the PR's branch or a one-line workflow change on main first — read `.github/workflows/*.yml` for what the node step does. Merge each PR only when its checks are green, with `gh pr merge --squash --delete-branch` (or `--merge` if the repo setting forbids squash; check `gh repo view --json mergeCommitAllowed,squashMergeAllowed`). Don't bypass a required check.
2. `git -C … pull --ff-only origin main` after each merge; the full local gate after the last (`bundle exec rspec`, `bundle exec srb tc`, `bundle exec ruby bin/generate` clean, each its own Bash call).
3. Then the remaining npm advisories: in `spec/support/federation`, `npm audit` again; fix what's left with `npm update <pkg>` for transitive packages within their ranges, and `overrides` in package.json only where the vulnerable package is pinned below the patched version by a dependency's own range (uuid ≥11.1.1 and @opentelemetry/core ≥2.8.0 are likely candidates). `@apollo/server` may move to `^5` if `gateway_static.mjs`/`gateway.mjs` still run on it (check the import surface; v5 changed `startStandaloneServer` little, but verify by running). Commit `package.json` + `package-lock.json` together.
4. Prove the harness still works: `INTEGRATION=1 bundle exec rspec spec/integration/router_parity_spec.rb` (must stay 73 identical / 2 refused / 0 wrong), and `bundle exec ruby bin/federation-diff`. Push main. Then `gh workflow run integration.yml --ref main -R dpep/graph_weaver` and `gh run watch` it; and the Ruby workflow the push triggers.
5. `gh api repos/dpep/graph_weaver/dependabot/alerts --jq '.[] | select(.state=="open") | .number'` — list what is still open and why (an advisory with no patched version within our constraints is a report, not a failure).

Report: per PR what failed, what you did, merge sha; the npm changes with before/after versions of each vulnerable package; parity and federation-diff results; both workflow conclusions; remaining open alerts with reasons; `git status` clean.
