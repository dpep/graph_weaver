# Lane: senior N's security findings — the channels that carry text the library didn't author

Read /tmp/claude/graph_weaver/brief-common.md for toolchain, gate, trailers. Baseline: main at the sha in your launch message (worktree off it). This is 0.7.1 work: CHANGELOG gets a `###  Unreleased` heading above `###  v0.7.0  (2026-09-13)` with your bullets in one contiguous block (a packaging lane adds its own block under the same heading; keep both on rebase). Don't touch version.rb. Every gate command its own Bash call. The senior's report is the final message of `/private/tmp/claude-501/-Users-dpepper-code-lib-ruby-graph-weaver/bb134e57-46b8-4797-a396-c7f5afad21be/tasks/a3d722a1685501b42.output` (`tail -c 16000`), log `/tmp/claude/graph_weaver/senior-log-N.md`, harnesses under `/tmp/claude/graph_weaver/senior-app-N/` (`probe/` bare bundle, `rails/secapp/` a real Rails app, `drive.rb` reproduces F1, `probe/p6_endpoint.rb` reproduces F2). Copy the harnesses, repoint their Gemfiles at your worktree, run each repro before and after.

You own the redaction boundary, and the senior's diagnosis is the brief: redaction was done value-first (which values are secret) and never channel-first (which channels carry text the library didn't author). Enumerate the channels once — `InputError` detail, `GraphQLError#message`, the `ServerError` body snippet, `extensions.code`, a redirect `Location`, a header value, anything else you find by grepping `lib/` for `raise`/`message`/`logger`/`warn`/`inspect` — give each a stated policy, and write the spec that keeps it true. If a fix needs a rule with an exception, say so and stop.

**F1 (HIGH) — a non-2xx body is spliced into `ServerError#message`, which `Error#initialize` logs at WARN.** A 500 page that echoes the request (Rails' own dev error page, many proxies) puts the request's variables — a password — and our own `Authorization` header into the production log. The url in the same sentence is scrubbed. Fix the rule, not the instance: text the library didn't author is capped at `VALUE_LIMIT` and, when it parses as JSON, run through `filter_variables`; when it doesn't, it is not spliced into `#message` at all — keep it on `#body` (already there) and let `#message` be status + hint + url, the way `#to_h` already keeps headers off. Decide which of those two and say why; the second is one sentence ("a message never carries a body") and I lean to it, with the first 200 bytes shown only at debug through `Redact`. Fix docs/logging.md's three claims the senior quotes and name the channel there. Spec: the senior's exact 500 page through `:live` against a raw server, asserting the literal password and bearer token appear nowhere in the log at any level, the message, or `to_h`; watched failing.

**F2 (MEDIUM) — `Testing::Endpoint#with_context` assigns `@client.context =` on shared state.** Eight concurrent requests each with its own Authorization: seven served another request's identity. The class's stated purpose is identity propagation. Fix by passing the resolved context into the execute call (the client contract takes `context:`? check `InProcess#execute`), else hold it in a Fiber/Thread-local for the duration, else a Mutex around dispatch — prefer the first that needs no shared mutation. Spec: the senior's 8-thread probe as an example, watched failing.

**F3 (LOW-MED) — `extensions.code` interpolated into the info log line.** A newline forges a second line; the same string becomes an APM tag. Strip control characters (or `inspect`) where the payload's `code`/`error` parts are built, once, so the subscriber and the APM tag both get it.

**F4 (LOW-MED) — `filter_parameters = []` also turns off url query-credential scrubbing.** `Endpoint.scrub_query` asks `Redact.filtered?`, which reads the app's list. A url credential is a credential whatever the logging appetite: scrub query params against the default list (or the cassette scanner's `CREDENTIAL_SHAPES`) regardless of `filter_parameters`, the way userinfo is already unconditional. One rule, stated in docs/logging.md.

**F5 (LOW)** — `transport/http.rb`'s "TLS options need an https url — got #{url}" bypasses `Endpoint.safe`. Fix and grep for any other url interpolation that does.

**Also in your files:** `require "graph_weaver/log_subscriber"` standalone raises NameError without `active_support/log_subscriber` loaded (junior 15); add the require inside so it is standalone-safe.

**The spec that keeps it true:** a shared example driving one secret (a password variable, a bearer `auth:`, an `?access_token=`, and a hostile `extensions.code` with a newline) through every channel you enumerated — live 500 with an echoing body, a 200 with errors, a redirect, a transport failure, a cassette write, the APM payload, `inspect` of every public object — and asserting the literal secret appears in none of the log at debug, no exception message, no `to_h`. Keep the list of channels in one place in the spec with a sentence per channel saying its policy, so the next channel someone adds has a row to fill.

Ownership: `lib/graph_weaver/errors.rb`, `lib/graph_weaver/logging.rb`, `lib/graph_weaver/log_subscriber.rb`, `lib/graph_weaver/internal/endpoint.rb`, `lib/graph_weaver/internal/redact.rb` if it exists, `lib/graph_weaver/transport.rb`, `lib/graph_weaver/transport/**`, `lib/graph_weaver/testing/endpoint.rb`, `lib/graph_weaver/in_process.rb` (only if F2 needs context: on execute), `spec/errors_spec.rb`, `spec/logging_spec.rb`, `spec/log_subscriber_spec.rb`, `spec/transport*_spec.rb`, `spec/http_spec.rb`, `spec/endpoint_spec.rb`, `spec/redaction_spec.rb` (new), `docs/logging.md`, `docs/testing.md` (the Endpoint concurrency sentence only), `CHANGELOG.md` (your block), `spec/support/public_surface.txt` via `bin/public-surface --update`. Not yours: `graph_weaver.gemspec`, `README.md` (the packaging lane), codegen, rspec.rb, tasks.

Gate: brief-common's (rspec random seed, `srb tc`, `bin/generate` clean), two random seeds, `INTEGRATION=1 bundle exec rspec spec/integration` once, and the senior's `drive.rb` and `p6_endpoint.rb` before/after pasted. Report: the channel list with each policy; per finding with messages verbatim; shas; `git status` clean.
