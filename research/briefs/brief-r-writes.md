# Lane: senior R's write-path findings

Read /tmp/claude/graph_weaver/brief-common.md for toolchain, gate, trailers. Baseline: main **df7043b** (worktree off it). 0.7.1 work: your bullets in one contiguous block under the existing `###  Unreleased` heading in CHANGELOG.md (other blocks are there; keep all on rebase). Every gate command its own Bash call. Senior R's report is the final message of `/private/tmp/claude-501/-Users-dpepper-code-lib-ruby-graph-weaver/bb134e57-46b8-4797-a396-c7f5afad21be/tasks/a410ca83fedd37ebc.output` (`tail -c 10000`), log `/tmp/claude/graph_weaver/senior-log-R.md`, probes under `/tmp/claude/graph_weaver/senior-app-R/` (`probe_retry_after_envelope.rb`, `probe_logging.rb`, `probe_nested_and_upload.rb`, `spec/harness_spec.rb`). Copy the app, repoint its Gemfile, run each probe before and after.

You own the write path's honesty: a payments engineer needs the client to speak where it is quiet. One rule per fix; if a fix needs an exception, say so and stop.

**R1 (HIGH) — `Retry-After` is discarded when the failure arrives as an envelope.** A 429 with `Retry-After: 7` and a GraphQL errors body backs off `[1.0, 2.0]`; the same status with no body backs off `[7.0, 7.0]`. The K transport lane left this deliberately ("carrying headers through would widen Envelope past the one question Retry asks") — the checkout engineer's answer is that a rate limiter's explicit number is exactly the question. Carry `retry_after` (the parsed seconds, not the headers) on `Transport::Envelope` beside `http_status`, and have `Retry#delay` read it from a returned envelope the way it reads it from a raised `ServerError` — one rule: "a server's Retry-After wins over the backoff, however the failure arrived". Spec at a real socket, both shapes, watched failing.

**R5 (MEDIUM) — a `File` in a variable is refused over the wire and accepted silently under `:in_process`.** The refusal sits beside `Wire.json`'s NaN rescue, which `:in_process` never reaches. Move it to where variables are serialized before dispatch (the generated module's variable serialization / `InputStruct#serialize` / `Coerce.variable` — find the one place every mode passes through) so every mode refuses with the same sentence. Spec: `:in_process`, `:fake`, `:wire`, live, all refuse; watched failing for `:in_process`.

**R4 (MEDIUM) — the `execute.graph_weaver` payload never says mutation.** `Wire.mutation?` already computes it for `Retry`. Add `kind: :query | :mutation | :subscription` to the payload (docs/logging.md's contract table, the LogSubscriber line if it fits), one source of truth with `Retry`'s check. Spec the payload.

**R3 (HIGH for the person, a design call for you) — `:fake` fabricates a non-empty `userErrors` beside a fabricated `order`.** A `PlaceOrderPayload { order, userErrors }` under `:fake` with no pins returns both, a shape that can't happen for real, so the natural happy-path assertion is flaky until pinned. Options: (a) docs only — say to pin `userErrors` to `[]`, the fake fabricates every list non-empty by design; (b) a convention: a list field whose name ends in `errors` (Relay/Shopify `userErrors`, `errors`) fabricates empty unless pinned — a rule with a name pattern, and the project has taken such conventions before when they match the ecosystem (reserved names, kind suffixes). Decide, say why, and if (b), state it in one sentence in docs/testing.md and spec it; if (a), the sentence goes beside the mutation paragraph.

**R2 (docs) — `QueryError#data` is nil when a `null: false` payload field's resolver raises**, by GraphQL null propagation; docs/errors.md claims without caveat that a mutation that created the order and failed on the way out leaves the data on `QueryError#data`. Add the caveat with the non-null payload shape as the example and what to do (make the payload field nullable, or read the order back).

**R6 (LOW, docs)** — `"10.00"` → `"10.0"` through a BigDecimal Money is numerically equal and textually different; one sentence in docs/scalars.md beside the money shapes.

Ownership: `lib/graph_weaver/transport.rb`, `lib/graph_weaver/retry.rb`, `lib/graph_weaver/query_module.rb`, `lib/graph_weaver/logging.rb` (payload only), `lib/graph_weaver/log_subscriber.rb`, `lib/graph_weaver/input_struct.rb`, `lib/graph_weaver/coerce.rb`, `lib/graph_weaver/internal/wire.rb` or wherever `Wire` lives, `lib/graph_weaver/testing/fake_client.rb` (R3 only), `spec/retry_spec.rb`, `spec/transport*_spec.rb`, `spec/logging_spec.rb`, `spec/log_subscriber_spec.rb`, `spec/input_struct_spec.rb`, `spec/testing_spec.rb`, `spec/in_process_spec.rb`, new specs, `spec/generated/**` via `bin/generate`, `docs/errors.md`, `docs/logging.md`, `docs/testing.md`, `docs/scalars.md`, `CHANGELOG.md` (your block), `spec/support/public_surface.txt` via `bin/public-surface --update`. Five read-only agents are running against main; nothing else writes.

Gate: brief-common's (rspec random seed, `srb tc`, `bin/generate` clean), two random seeds, `INTEGRATION=1 bundle exec rspec spec/integration` once, and the senior's probes before/after pasted. Report: per finding with the rule and messages verbatim; R3's decision and why; shas; `git status` clean.
