# Lane: senior J — list_size compounds across nested lists, plus three performance sentences

Read /tmp/claude/graph_weaver/brief-common.md for toolchain, gate, trailers. Baseline: main **0df590a** (worktree off it). CHANGELOG: your bullets in one contiguous block under the existing `###  Unreleased` heading (other lane blocks are there; keep all on rebase). Every gate command its own Bash call. Senior J's log: `/tmp/claude/graph_weaver/senior-log-J.md` (app `/tmp/claude/graph_weaver/senior-app-J`, measured with CPU time and `GC.stat` allocation counts because the box was loaded — do the same).

**#1 (MEDIUM-HIGH) — `FakeClient` `list_size:` applies to every unbounded list the walk reaches, so a row type with a nested list fabricates n×n leaves.** `list_length` (`testing/fake_client.rb` ~581) falls back to the one global `@list_size` for each list; allocations per row doubled with every doubling of `list_size` (1,088 → 13,009 objects/row from 100 → 1600) on `Row { owner { ... } tags }` with `tags` uncapped; capping `tags(first: 3)` in the query flattened it. Fix, matching the pattern pins already use: `list_size:` also accepts a Hash keyed the way pins are — a `"Type.field"` coordinate or a bare field name — with a default under a key you choose (or the Integer/Range form stays the default for everything unnamed). Reuse the `overrides`/pin fallback shape (`fetch(coordinate) { fetch(name) { default } }`) rather than a second lookup path; the Integer, Range and Hash forms are one rule: "how long an unbounded list is, per coordinate, defaulting to the whole". Refuse a Hash key the schema doesn't know the way pins refuse one. Same on `config.list_size` and the `graphql_fake(list_size:)` helper if they exist. Spec: allocations or leaf counts flat in the nested list when it is named, watched failing first; and the refusal.

**Docs (yours):** `docs/testing.md` beside `config.list_size = 1..3`: the multiplication across nested lists, and the Hash form. `docs/scalars.md` where a timestamp registration is discussed: `DateTime` casts about 2.4× slower than `Date` and several times slower than `Time` (Rational calendar arithmetic vs a C timestamp) — say `Time` for timestamps, one sentence with the measured ratio rounded. `docs/generated_modules.md` (or wherever `verify_generated!` is documented): it recomputes the whole plan and costs what `generate!` costs regardless of what changed, so it is a once-per-suite check, not a per-example assertion. And one sentence in `docs/generated_modules.md`'s Sorbet paragraph: `T::Configuration.default_checked_level = :never` buys nothing for `from_h` because `T::Struct` prop validation is `.checked(:never)` in sorbet-runtime itself and the generated sigs already opt out.

Ownership: `lib/graph_weaver/testing/fake_client.rb`, `lib/graph_weaver/internal/values.rb`, `lib/graph_weaver/internal/overrides.rb` (if the Hash form shares its lookup), `lib/graph_weaver/testing.rb` (`config.list_size` only), `lib/graph_weaver/rspec.rb` (`list_size:` on the helper only — another lane touched this file recently; rebase), `spec/testing_spec.rb`, `spec/fake_client_spec.rb` if it exists, `spec/rspec_spec.rb` (list_size examples only), new specs, `docs/testing.md` (the list_size paragraph only — another lane is adding a redaction paragraph elsewhere in the file; rebase), `docs/scalars.md`, `docs/generated_modules.md`, `CHANGELOG.md` (your block), `spec/support/public_surface.txt` via `bin/public-surface --update`. Not yours: `testing/router.rb`, `internal/planner.rb`, `tasks.rb`, `transport/**`, `retry.rb`, `errors.rb`, `docs/federation.md`, `docs/errors.md`, `docs/transports.md`.

Gate: brief-common's (rspec random seed, `srb tc`, `bin/generate` clean), two random seeds. Report: the list_size rule in one sentence; the before/after allocation counts on the senior's shape; messages verbatim; shas; `git status` clean.
