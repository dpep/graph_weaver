query($term: String!) {
  search(term: $term) {
    __typename
    ... on Named {
      name
    }
    ... on Person {
      birthday
    }
    ... on Pet {
      ...PetFields
    }
  }
}

fragment PetFields on Pet {
  species
}
