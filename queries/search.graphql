query($term: String!) {
  search(term: $term) {
    __typename
    ... on Person {
      name
      birthday
    }
    ... on Pet {
      ...PetFields
    }
  }
}

fragment PetFields on Pet {
  name
}
